# 롱폼 영상 자동화 — 계획 (v2)

> **상태: 계획만. 코드는 한 줄도 건드리지 않았습니다.** (2026-08-21, Gim 지시)
> ⛔ 착수는 §7 의 결정 세 가지를 Gim 이 정한 뒤입니다.
>
> **v2 (2026-08-21 재검토) — 추정이던 것을 실측으로 바꾼 판입니다.** 상세 근거는
> `research.md` 「2026-08-21 롱폼 계획서 재검토」 절. 바뀐 곳 넷입니다.
> 1. **화면 조달에 안 E(위키미디어 커먼즈 실사)를 추가**했습니다 — 스톡이 깨진 8개 대상 중
>    **7개를 $0 실사로 덮는다**는 것을 API 로 확인했습니다. 권고 조합이 바뀌었습니다(§2).
> 2. **렌더 시간을 실측**했습니다 — xfade 체인은 20장면 이후 **평평**합니다. 폭발 걱정 기각(§1).
> 3. **§5 의 「롱폼에만 필요한 것」 7개 중 진짜 새 작업은 썸네일 하나**입니다. 나머지는
>    한 줄~작은 수정이었습니다. 구현 규모를 과대평가하고 있었습니다.
> 4. **교차 게시 실험은 Gim 결정으로 중단**됐습니다. 「결과를 기다린다」는 서술을 전부 뺐습니다.

## 0. 왜 롱폼인가 — 근거와 반대 근거

**근거(실측)** — 최근 8일 채널 유입의 **81.4%가 유튜브 검색**이었습니다. 쇼츠 피드는
13.6%로 죽었지만 **검색 경로는 살아 있습니다.** 롱폼은 검색·추천이 주력이고 제목·설명·챕터가
실제로 작동합니다. **막힌 문이 아니라 열린 문 쪽입니다.**

**반대 근거** — ① 「스톡+TTS 10분」은 양산형 콘텐츠 서술에 쇼츠보다 **더 노골적으로** 들어맞습니다.
② 11초에서도 절반이 이탈했습니다(참여도 47.7%). ③ 편당 원가·시간이 30~50배입니다.

**⛔ 확정된 전제 — 「같은 형식을 길게」는 실패합니다.** 쇼츠는 장면 5개 중 **40%가 대사와
안 맞았습니다**. 8분이면 장면이 40~119개이고, 같은 비율이면 화면 절반이 엉뚱합니다.
**롱폼의 성패는 「화면을 어떻게 조달하느냐」 하나에 달려 있습니다.** §2 가 이 문서의 핵심입니다.

---

## 1. 실측 기준선 (외삽의 근거)

기존 산출물 25편에서 잰 값입니다. **장면당 4.02초 · 초당 6.42자 · 렌더는 실시간의 1.63배
(64편 19.9분 실측) · 0.49MB/초.**

| 항목 | 쇼츠(실측) | 8분 | 10분 | 15분 |
|---|---|---|---|---|
| 장면 수(현행 밀도) | 5.0 | 119 | 149 | 224 |
| 대본 글자 | 128자 | 3,083 | 3,854 | 5,780 |
| 렌더 시간 | 19초 | 13분 | 16분 | 24분 |
| 파일 용량 | 5.6MB | 234MB | 292MB | 438MB |

**✅ v2 실측 — 렌더 폭발 걱정은 기각됐습니다.** `concat_xfade` 만 떼어 실제 렌더물로
장면 수를 늘려가며 쟀습니다(초당 렌더 비용, 낮을수록 빠름).

| 장면 수 | 5 | 10 | 20 | **40** |
|---|---|---|---|---|
| 실시간 배수 | 0.31x | 0.39x | 0.44x | **0.43x** |

**20장면을 넘으면 평평합니다.** 5→40 에서 +39% 오르고 멈춥니다. 전체 렌더는 1.63x 에
이 증가분을 얹어 **약 1.75x**, 8분물이면 **약 14분**입니다(위 표 13분과 거의 같음).
⚠ 다만 이건 **조립 구간(전체의 약 1/4)** 만입니다. 나머지는 장면별 직렬 루프라 선형입니다.
⚠ **149장면은 여전히 안 돌려 봤습니다.** 40장면까지만 확인했습니다.

---

## 2. 화면 조달 — 네 가지 안 전면 비교 ⭐

**8분(480초) 기준**으로 통일해 비교합니다. 장면 길이를 12초로 잡으면 **40장면**입니다.
(현행 4초를 유지하면 119장면이라 어느 안으로도 감당이 안 됩니다. **장면을 늘리는 것은
모든 안의 공통 전제**입니다.)

### 안 A — 스톡 장면을 길게 (현행 확장)

- **방법**: Pexels 클립을 40개만 받고 장면당 12초. 켄번스(느린 확대·이동)로 정적임을 덮음.
- **원가**: **$0**
- **구현**: `video.py` 상수 조정 + 켄번스 필터. **가장 작음**
- ⛔ **치명적 약점**: 불일치 40%가 그대로입니다. 오히려 **한 화면이 12초씩 노출**되므로
  틀린 화면이 더 오래 보입니다. 쇼츠에서 2.8초 스쳐 지나가던 것이 **12초 박혀 있습니다.**
- **판정**: 단독으로는 부적합. **다른 안의 배경 채움용**으로만.

### 안 B — AI 이미지 + 프로그램 모션 ⭐ **비용 대비 최선**

- **방법**: 장면마다 대사에 맞는 이미지를 **생성**하고, ffmpeg 켄번스·패럴랙스로 움직임을 만듦.
- **원가**: 40장 기준

  | 모델 급 | 장당 | 편당 | 월 30편 |
  |---|---|---|---|
  | 저가(GPT Image Mini·오픈웨이트 호스팅) | $0.005~0.008 | **$0.20~0.32** | **$6~10** |
  | 표준(Imagen 4 Fast/Standard·Flux 2 Pro) | $0.02~0.04 | **$0.80~1.60** | **$24~48** |

- **강점**: **화면에 대상이 없는 일은 사라집니다.** 대사에 맞는 그림을 만들어 내니까요.
- **약점**: 정지 이미지라 **진짜 움직임이 없습니다.** 켄번스는 흉내입니다.
  인물·손 같은 것은 AI 이미지 특유의 어색함이 나옵니다.
- ⛔ **v2 에서 새로 짚은 치명적 약점 — 실존 대상을 「날조」합니다.**
  AI 가 만든 「데스밸리」는 데스밸리의 사진이 아니고, AI 가 만든 「벌새」는 해부학이 틀릴 수
  있습니다. **이 프로젝트 규칙은 「모든 콘텐츠는 사실 기반」입니다.** 과학·상식 채널에서
  실존 대상을 생성 이미지로 채우는 것은 불일치를 **「그럴듯한 날조」로 바꾸는 것**이지
  해결이 아닙니다. → **실존 대상은 안 E 로, AI 는 추상·개념·과정에만.**
- **구현**: 이미지 생성 모듈 + 켄번스 필터. **중간 규모**

### 안 C — AI 생성 영상 (Gim 요청으로 별도 검토)

**C-1. 전량 AI 영상**

| 모델 | 초당 | 480초(8분) | 월 30편 |
|---|---|---|---|
| Wan 2.6 | $0.05 | **$24** | **$720** |
| Kling 3.0 | $0.029~0.09 | $14~43 | $420~1,290 |
| Seedance 2.0 fast | $0.09~0.14 | $43~67 | $1,290~2,010 |
| Veo 3.1 Fast | $0.15 | $72 | $2,160 |
| Runway Gen-4.5 | $0.20 | $96 | $2,880 |

⛔ **최저가로도 월 $720(약 100만 원)입니다. 하루 1편 자동화로는 성립하지 않습니다.**
게다가 대부분 모델의 **1회 생성 상한이 5~15초**라 480초를 채우려면 **40~96회 생성**을
이어 붙여야 하고, **컷마다 화풍·인물이 튑니다**(일관성 유지가 별도 과제).

**C-2. 하이브리드 — 핵심 장면만 AI 영상** ✅ **이쪽은 성립합니다**

- 40장면 중 **도입 1 + 전환점 2~4 + 마무리 1** 만 AI 영상, 나머지는 안 B 이미지.

  | AI 영상 장면 수 | 초 | 편당(Wan $0.05) | 월 30편 |
  |---|---|---|---|
  | 3장면 | 36초 | **$1.80** | **$54** |
  | 6장면 | 72초 | **$3.60** | **$108** |

- **강점**: 시청자가 가장 이탈하는 **도입 15초**에 진짜 움직이는 화면을 넣을 수 있습니다.
- **판정**: **성과가 확인된 뒤에 붙이는 2차 옵션.** 1차부터 넣지 않습니다.

**C-3. 지금 연결된 Higgsfield MCP 로 되는가 — ⛔ 안 됩니다(현재 상태로는)**

- **실측**: 플랜 `free` · **잔액 8 크레딧** · 무제한(unlim) **사용 불가**.
- 쓸 수 있는 영상 모델은 많습니다(Seedance 2.0/2.5, Kling 3.0, Wan 2.6, MiniMax H3,
  Grok Video 1.5, FLUX 3 Video, Cinema Studio 3.0 등). **모델이 없는 게 아니라 크레딧이 없습니다.**
- 대부분 **생성 상한 4~15초**입니다. 롱폼은 이어 붙이기가 전제입니다.
- **결제 플랜 없이는 검증조차 못 합니다.** §7 준비물에 넣습니다.

### 안 E — 위키미디어 커먼즈 실사 ⭐ **v2 신설 · 실존 대상 전담**

- **방법**: 커먼즈 검색 API 로 **재사용 가능 라이선스(퍼블릭도메인·CC0·CC BY)** 고해상도
  사진만 받아 켄번스. 설명란에 저작자·라이선스를 자동 표기.
- **원가**: **$0**
- ⭐ **왜 이게 중요한가** — 08-16 에 확인한 불일치 8편의 대상은 **전부 「실존하는 특정
  개체·장소·현상」**이었습니다(벌새·나무늘보·무지개·번개·데스밸리·사막의 밤·탄산·우주).
  Pexels 세로 라이브러리에 그런 것이 없어서 깨졌습니다. **커먼즈가 바로 그 영역입니다.**

  **실측 (대상별 상위 20건 중 재사용OK + 1920x1080↑)**

  | 벌새 | 나무늘보 | 데스밸리 | 번개 | 무지개 | 사막의 밤 | 성운 | 동굴 | 꿀단지 | 탄산 |
  |---|---|---|---|---|---|---|---|---|---|
  | **7** | **8** | **3** | **4** | **3** | **17** | **18** | **1** | 5 | **0** |

  **깨졌던 8개 중 7개가 1건 이상 확보됩니다.** 0건인 「탄산 거품」은 **Pexels 가 이미 잘
  맞히던 흔한 사물**입니다 — 두 저장소가 정확히 상보적입니다. 전체 통과율 66/185 = **36%**.
- ⭐ **관련성 검사가 파라미터로 붙습니다.** 커먼즈 구조화 데이터의 `P180`(묘사한다)로
  「이 파일은 X 를 담고 있다」고 **사람이 태그한 것만** 받을 수 있습니다
  (`haswbstatement:P180=Q33741` → 번개 사진). `fetch_video()` 에 없던 바로 그 검사입니다.
- **약점 — 정직하게**
  - **CC BY 는 저작자 표시 의무**가 있습니다. CC BY-SA(동일조건변경허락)는 영상 전체의
    라이선스 해석이 얽히므로 **아예 걸러야 합니다**(위 집계에서 제외했습니다).
  - **아주 희귀한 대상은 여유가 없습니다** — 「Krubera cave」는 1건뿐이었습니다.
  - P180 은 **정밀도는 높지만 재현율이 부분적**입니다. 실무는 **P180 우선 → 일반 검색 폴백** 2단.
  - **가로 정지 사진**이 대부분입니다. 세로 쇼츠엔 안 맞고 **16:9 롱폼엔 맞습니다.**
  - ⛔ **아직 사진을 눈으로 보지 않았습니다.** API 응답 수만 셌습니다. 08-16 의 교훈
    (숫자만 보고 산출물을 안 봄)이 그대로 적용됩니다 — **1단계에서 반드시 눈으로 봅니다.**
- **구현**: 검색·라이선스 필터·저작자 표기. **안 B 와 같은 켄번스 경로를 공유합니다.**

### 안 D — 그래픽·자막 중심 (스톡 최소)

- **방법**: 도표·숫자 카운터·지도·타임라인·비교표를 코드로 그려 화면을 채움.
- **원가**: **$0**
- **강점**: 복제가 어렵고 정보 밀도가 높습니다. **양산형 판정에서 가장 멉니다.**
  「스톡 슬라이드쇼」가 아니라 **자체 제작물**입니다.
- **약점**: **만드는 코드가 가장 많습니다.** 그리고 주제가 **숫자·비교·순위·연표**형으로
  제한됩니다(「왜 하늘이 파란가」는 도표로 못 만듭니다).
- **판정**: 주제군을 그쪽으로 고정할 수 있다면 **가장 강한 안**입니다. 장르 결정과 묶여 있습니다.

### 2-x. 종합 비교

| | A 스톡 | B AI 이미지 | **E 커먼즈 실사** | C-1 AI영상 전량 | C-2 하이브리드 | D 그래픽 |
|---|---|---|---|---|---|---|
| 월 원가(30편) | $0 | $6~48 | **$0** | $720~2,880 | $54~108 | $0 |
| 불일치 해결 | ❌ 그대로 | ✅ | ✅ | ✅ | ✅ | ✅ |
| **사실성** | △ 실사지만 딴것 | 🔴 **날조** | ✅ **실사** | 🔴 날조 | 🟡 혼합 | ✅ |
| 잘 맞는 대상 | 흔한 사물 | **추상·개념** | **실존 개체·장소** | 무엇이든 | 무엇이든 | 숫자·비교 |
| 진짜 움직임 | △ 스톡 | ❌ 켄번스 | ❌ 켄번스 | ✅ | △ 일부 | ❌ |
| 양산형 위험 | 🔴 높음 | 🟡 중간 | 🟡 중간 | 🟢 낮음 | 🟢 낮음 | 🟢 가장 낮음 |
| 구현 규모 | 소 | 중 | **중** | 대(일관성) | 대 | **최대** |
| 별도 의무 | 없음 | 없음 | 🟡 **저작자 표시** | 없음 | 없음 | 없음 |

### ⭐ v2 권고 — 하나를 고르는 게 아니라 **대상 유형으로 갈라 씁니다**

어느 안도 단독으로는 전 영역을 못 덮습니다. **깨진 자리와 잘 맞는 자리가 안마다 다릅니다.**

| 대사가 가리키는 것 | 조달원 | 근거 |
|---|---|---|
| **실존 특정 개체·장소·천체·현상**(벌새·데스밸리·성운) | **E 커먼즈 실사** | 08-16 에 깨진 자리. $0·사실 기반 |
| **흔한 사물·분위기·배경**(꿀·얼음·양파) | **A Pexels 스톡** | 이미 45% 적중하던 영역 |
| **추상·개념·과정·비유**(「시간이 흐른다」) | **B AI 이미지** | 어느 저장소에도 실물이 없음 |

**대본 3번째 칸(검색어)에 대상 유형을 함께 적게 하면 갈래가 자동으로 정해집니다.**
성과가 확인되면 **도입부에만 C-2 를 얹습니다.** D 는 장르를 숫자·비교형으로 좁힐 때만.
⚠ **이 배분으로 AI 이미지 장수가 줄어 §3 원가는 아래 표보다 내려갑니다.**

---

## 3. 원가 총계 (8분 · 월 30편 · 권고안 B 기준)

| 항목 | 편당 | 월 |
|---|---|---|
| 대본 생성(Anthropic) | $0.05~0.15 | $1.5~4.5 |
| 음성(Google TTS Neural2) | **$0** (월 100만 자 무료 = 259편) | **$0** |
| 화면(AI 이미지 40장, 표준급) | $0.80~1.60 | $24~48 |
| 배경음악(Jamendo) | $0 | $0 |
| 렌더·저장(로컬) | $0 | $0 |
| **합계** | **$0.85~1.75** | **$26~53** |

- C-2 를 얹으면 **+$54~108/월**.
- ⚠ **실측이 아니라 공개 단가표 기반 추정입니다.** 실제 청구는 1단계에서 1편 만들어 확인합니다.

**v2 정정 — 단가 재확인(2026-08-21 조회) · 배치가 절반입니다**

| 모델 | 장당 | v1 값 | |
|---|---|---|---|
| Imagen 4 Fast | $0.02 | $0.02~0.04 | 그대로 |
| Imagen 4 Standard | $0.04 | $0.02~0.04 | 그대로 |
| Gemini 3.1 Flash Lite Image **(배치)** | **$0.0168** | — | 🆕 최저가 |
| Gemini 2.5 Flash Image **(배치)** | $0.0195 | — | 🆕 |

🆕 **배치 요청이 정확히 절반입니다.** 밤에 자동으로 도는 파이프라인이라 **대화형 응답 속도가
필요 없습니다.** 배치를 기본으로 둡니다.
⚠ v1 의 「저가 모델이면 월 $8~15」는 `gpt-image-1-mini` 를 **중간 화질**로 쓸 때 성립합니다.
토큰 과금이라 해상도·화질을 올리면 그대로 안 됩니다.

### ⛔ v2 에서 뒤집은 것 — **원가가 화면 밀도를 정하고 있었습니다**

v1 은 「40장면 × 12초」로 잡았는데, 그 12초는 **시청 유지 근거가 아니라 원가를 맞추려고 나온
값**입니다. Imagen 4 Fast 로 다시 재면:

| 장면 길이 | 장수 | 편당 | 월 30편 |
|---|---|---|---|
| 12초 | 40 | $0.80 | $24 |
| 6초 | 80 | $1.60 | $48 |
| 4초(현행 쇼츠 밀도) | 120 | $2.40 | **$72** |

**가장 촘촘하게 가도 월 $72 입니다.** 게다가 §2 의 배분을 쓰면 실존 대상은 커먼즈($0)로
빠져 이보다 더 내려갑니다. **정지 이미지를 12초씩 미는 게 견딜 만한지는 원가가 아니라
시청 유지로 정해야 합니다.** 1단계에서 눈으로 보고 정합니다.

---

## 4. 파이프라인 설계 (모듈 단위 — 한 파일에 몰지 않습니다)

기존 `video.py` 863줄에 다 넣으면 무너집니다. **새 기능은 새 모듈**로 두고
`video.py` 는 조립만 담당하게 합니다.

```
longform/
  script.py     대본 생성 — 3,000~4,000자, 도입·본론·결말·챕터 구조
  visual.py     화면 조달 — 대상 유형별 분기(커먼즈 실사 / 스톡 / AI 이미지) + 켄번스
  audio.py      음성(44.1kHz) + 음악 이어붙이기(현 트랙 최장 3.6분)
  assemble.py   장면 조립·전환·자막·챕터 타임스탬프
  thumb.py      썸네일 생성 + 업로드 (현재 코드에 아예 없음)
```

**의존 방향은 한쪽입니다** — `assemble` → (`script`·`visual`·`audio`), 역방향 금지.
기존 `youtube.py` 의 `upload()` 는 title·description·tags·category 를 이미 받습니다
(`youtube.py:45`). **썸네일 설정만 없어 추가가 필요합니다.**

⭐ **v2 — 썸네일에 OAuth 재인증이 필요 없습니다.** 공식 문서상 `thumbnails.set` 이 받는
스코프는 `youtube.upload`·`youtube`·`youtube.force-ssl`·`youtubepartner` 넷이고,
**우리는 앞의 둘을 이미 갖고 있습니다**(`youtube.py:11-15`). **Gim 이 할 일은 없습니다.**
제한은 **2MB · JPEG/PNG** 입니다.

---

## 5. 쇼츠와 다른, 롱폼에만 필요한 것

⛔ **v1 은 7개를 전부 `❌ 없음` 으로 적어 구현 규모를 크게 보이게 했습니다.**
코드를 열어 다시 재니 **진짜 새 작업은 썸네일 하나**입니다.

| 항목 | 왜 | **v2 실제 규모** |
|---|---|---|
| **썸네일** | 롱폼은 CTR 이 썸네일에서 갈립니다 | 🔴 **진짜 새 모듈.** 단 OAuth 재인증 불필요(§4) |
| **음악 길이** | 보유 트랙 **17곡 최장 3:33.86**, 10분 이상 0개 | 🟢 **`-stream_loop -1` 한 줄.** `add_music`(`video.py:689`)이 `amix=duration=first` 라 음악이 끝나면 그 뒤가 조용해질 뿐, **깨지지 않습니다** |
| **오디오 품질** | 현재 **24kHz mono 71~75kbps**(실측) | 🟢 **최종 인코드에 `-ar 44100 -ac 2 -b:a 128k` 한 줄** |
| **챕터 타임스탬프** | 시청 유지·검색에 직접 작동 | 🟢 작음. `concat_xfade` 가 이미 장면별 누적시간(`acc`)을 계산합니다(`video.py:731`) |
| **인트로/아웃트로** | 채널 정체성·구독 유도 | 🟢 장면 2개 추가 |
| **제목·설명 SEO** | 쇼츠와 달리 롱폼은 실제로 먹힙니다 | 🟢 필드 있음(`youtube.py:45`). 문자열 생성만 |
| **도입 15초** | 롱폼 이탈의 대부분이 여기서 납니다 | ⚪ 코드가 아니라 **대본 설계** 문제 |

⛔ **v1 의 「TTS 를 44.1kHz 로 올려야」는 틀렸습니다.** 구글 공식 문서가 *요청 샘플레이트가
음성의 원래 값과 다르면 변환하며 음질이 나빠질 수 있다*고 명시합니다. 내레이션은 원래
24kHz라 **올려도 정보가 안 늘어납니다.** **실제로 깎이고 있는 것은 음악**입니다 —
44.1kHz 스테레오 원본이 내레이션에 맞춰 24kHz 모노로 내려가고 있습니다. 그래서 고칠 곳은
TTS 가 아니라 **최종 인코드**입니다.

⚠ **v2 에서 새로 짚은 것 — 장면 처리가 직렬입니다.** `make_video`(`video.py:779`)가 장면을
하나씩 차례로 돕니다. 40장면이면 **TTS 40회 · 화면 조달 40회 · ffmpeg 40회를 순서대로**
기다립니다. 네트워크 왕복이 그대로 쌓입니다. 병렬화는 1단계 실측 뒤에 판단합니다.

---

## 6. 단계 계획 — 각 단계의 **확인 방법**을 먼저 적습니다

| 단계 | 할 일 | 확인 방법(통과 기준) | 규모 |
|---|---|---|---|
| **0** | §7 결정 3가지 | Gim 선택 | 대화 |
| **1** | **타당성 1편** — 대본 1편·화면 40장(커먼즈/스톡/AI 배분)·조립까지 손으로 관통 | ① 실제 청구액 확인 ② **화면 40장 전수 육안** — 불일치 0 **그리고 커먼즈 사진이 실제로 그 대상인지** ③ 12초 켄번스가 견딜 만한지 판단 | 스크립트 수준 |
| **2** | `script.py` — 3,000~4,000자 + 챕터 | 3편 뽑아 **사람이 읽어** 판정. 사실 오류·반복 없는지 | 새 모듈 |
| **3** | `visual.py` — 대상 유형별 분기(커먼즈 P180 → 커먼즈 검색 → 스톡 → AI) + 켄번스 | 40장 **전수 육안 검사**, 불일치 ≤2장. **라이선스가 PD·CC0·CC BY 인지 자동 확인** | 새 모듈 |
| **4** | `audio.py` + `assemble.py` — 44.1kHz·음악 연결·자막·챕터 | **1편 완주**, 렌더 시간·용량 실측 | 새+수정 |
| **5** | `thumb.py` — 썸네일 생성·업로드 | 실제 업로드 후 스튜디오 화면 확인 | 새 모듈 |
| **6** | **샘플 3편 비공개 업로드 → Gim 승인** | **화면 승인 게이트** | — |
| **7** | 자동화 재가동(**하루 1편**) | 스케줄러 재등록·3일 관찰 | — |

⛔ **1단계를 건너뛰지 않습니다.** §1 렌더 시간은 v2 에서 실측됐지만 **§3 원가와 커먼즈
적중률은 여전히 추정입니다.** 특히 **커먼즈 사진을 아직 한 장도 눈으로 보지 않았습니다** —
API 응답 수만 셌습니다. 08-16 에 「숫자만 보고 산출물을 안 봐서」 40% 불일치를 26일간 놓쳤고,
**같은 실수를 반복할 자리가 정확히 여기입니다.**

⛔ **2~5 는 파일 3개 이상을 만들므로 각각 착수 전 승인 게이트 대상입니다.**
⛔ **6 은 화면이 바뀌는 변경이라 Gim 승인 없이 공개하지 않습니다.**

---

## 7. Gim 이 정해야 할 것 (0단계)

| # | 결정 | 선택지 | 영향 |
|---|---|---|---|
| **①** | **화면 방식** | **E+A+B 배분(§2 권고)** / B 단독(AI 이미지) / D(그래픽) / A(스톡 유지) | 원가·구현 규모·**사실성** |
| **②** | **길이** | 8분 / 10분 / 15분 | 장면 수·렌더·원가 비례 |
| **③** | **채널** | 기존 「1분 궁금증」 / 새 채널 | 이력 승계 vs 초기화 |

**②에 대한 v2 주의 — 「8분=중간광고 하한」을 근거로 쓰지 마십시오.**
8분 하한 자체는 사실입니다(유튜브 공식 도움말). 그러나 중간광고는 **수익 창출이 켜져야**
붙고, 이 채널은 **구독 3명 · 누적 조회 214회**입니다. YPP 자격까지 한참 멉니다.
**길이는 광고가 아니라 시청 유지로 정해야 합니다.**

**③에 대한 v2 주의 — 판단 근거가 사라졌습니다.**
「기존 채널 이력이 문제인가」를 가르는 유일한 실험(교차 게시)이 **Gim 결정으로 중단**됐습니다.
이제 이 결정은 **데이터 없이** 내려야 합니다. ⛔ **「채널 이력 때문이다」라고 단정하지 마십시오.**

### 준비물 체크리스트 — Gim 개입이 필요한 것

| 준비물 | 필요 시점 | Gim 이 할 일 |
|---|---|---|
| **AI 이미지 API 키·결제** | 1단계 | 제공사 선택 후 키 발급 + 카드 등록. ⚠ §2 배분을 쓰면 AI 장수가 줄어 부담이 작아집니다 |
| ~~썸네일 OAuth 재인증~~ | — | ✅ **불필요합니다**(스코프 이미 보유, §4) |
| 위키미디어 커먼즈 | 1단계 | **키 없음·가입 없음.** 할 일 없습니다 |
| **Higgsfield 유료 플랜**(C-2 를 쓸 때만) | 2차 | 현재 free·8크레딧이라 **결제 필요** |
| 새 채널(③에서 고르면) | 6단계 | 브랜드 계정 생성 + OAuth 재인증 |
| 나머지(TTS·Pexels·업로드·디스크) | — | **이미 있습니다. 할 일 없음** |

---

## 8. 리스크와 **중단 기준**

시작하기 전에 「언제 접을지」를 정해 둡니다. 안 정하면 매몰비용으로 계속 갑니다.

| 리스크 | 중단·전환 기준 |
|---|---|
| 화면 불일치가 안 잡힘 | 3단계에서 40장 중 **불일치 3장 이상**이면 배분을 접고 D 로 |
| **커먼즈 사진이 실제로 안 맞음** | 1단계 육안에서 **실존 대상 커버율이 절반 미만**이면 안 E 를 접습니다(그러면 실존 대상은 답이 없어져 **장르를 추상·개념 쪽으로 옮겨야** 합니다) |
| **라이선스 사고** | CC BY-SA 나 표시 누락이 한 건이라도 나가면 **즉시 중단하고 전수 점검** |
| 원가가 추정을 크게 넘음 | 1단계 실측이 **편당 $3 초과**면 저가 모델로 내리거나 재검토 |
| 렌더가 감당 안 됨 | 8분 1편이 **40분 초과**면 길이를 줄이거나 화질을 낮춤 |
| 올려도 안 봄 | 공개 **10편 · 30일** 뒤 조회 중앙값이 **쇼츠(0회)와 같으면 중단** |
| 양산형 판정 | 경고·수익 제외 통지가 오면 **즉시 중단하고 재설계** |

---

## 9. 확인하지 못한 것 (정직하게)

**v2 에서 실측으로 옮긴 것 (더 이상 미확인이 아닙니다)**
- ~~40장면 렌더 시간~~ → **쟀습니다.** 20장면 이후 평평, 8분물 약 14분(§1).
- ~~썸네일 OAuth~~ → **재인증 불필요**(§4).
- ~~음악·오디오 규모~~ → **각각 한 줄 수정**(§5).
- ~~이미지 단가~~ → **확인. 배치가 절반**(§3).

**여전히 확인하지 못한 것**
- **커먼즈 사진을 눈으로 안 봤습니다.** API 응답 수만 셌습니다. **가장 중요한 미확인 항목**이며
  1단계 육안 검사의 핵심입니다. 지금 「커먼즈면 해결된다」고 단정하지 않습니다.
- **149장면 렌더.** 40장면까지만 쟀습니다.
- **AI 이미지 실제 청구액.** 공개 단가표 기반 추정입니다. 해상도·재시도로 달라집니다.
- **롱폼 시청 유지율.** 이 채널에 롱폼 이력이 없어 근거가 없습니다.
- **12초 켄번스가 견딜 만한지.** 원가로 정한 값이라 시청 유지 근거가 없습니다(§3).
- **경쟁 롱폼 채널 실측.** 같은 장르의 길이·구성·성적을 재지 않았습니다.
- **화풍 일관성.** 커먼즈 실사 + AI 이미지 + 스톡이 섞이면 **한 영상으로 안 보일 수 있습니다.**
  v1 은 AI 이미지끼리의 일관성만 걱정했는데, **배분을 쓰면 이 위험이 더 커집니다.**
  1단계에서 확인합니다.

**⛔ 영구 미확정으로 남은 것**
- **조회 부진이 콘텐츠 탓인지 채널 이력 탓인지.** 07-27 부터 미확정이었고, 그것을 가르는
  유일한 실험(교차 게시)이 **2026-08-21 Gim 결정으로 중단**됐습니다. 새 데이터 없이
  **추정으로 답을 만들지 않습니다.** 파일은 `G:\내 드라이브\쇼츠_교차게시\` 에 그대로 있어
  마음이 바뀌면 그날 재개할 수 있습니다.
