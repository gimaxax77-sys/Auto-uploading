# 자동 업로드 툴 — 계획

## 무엇을 왜 만드는가

주제를 주면 AI가 글을 쓰고, SNS에 자동으로 올리는 도구입니다.
플랫폼마다 성격이 전혀 다르므로 **한 곳을 끝까지 작동시킨 뒤 같은 틀에 하나씩 붙입니다.**

## 플랫폼별 성격

| 묶음 | 플랫폼 | 입력 | 상태 |
|---|---|---|---|
| 글만 | X (트위터) | AI 글 | **1차 목표** |
| 글만 | Threads | AI 글 | 2차 |
| 이미지 필수 | Instagram | 글 + 사진 | 보류 |
| 영상 필수 | YouTube, TikTok | 영상 파일 | 보류 (아래 참고) |

## 할 일

- [x] 저장소·브랜치 세팅
- [x] `.gitignore`
- [x] 글 생성 분리 (`generate.py`)
- [x] X 업로드 코드 (`main.py`)
- [x] 영상 생성 코드 (`video.py`) — 대본·음성·이미지·조립
- [x] 대본 파일 입력 (`--file`) — Anthropic 없이 영상 생성
- [x] 영상 1개 실제 생성 검증 (`sample.mp4`)
- [x] YouTube 업로드 코드 (`youtube.py`)
- [ ] Gim이 Pexels 키를 `.env`에 넣기 (발급 완료, PC 필요)
- [ ] Gim이 Google Cloud OAuth 설정 (`client_secret.json`, PC 필요)
- [ ] YouTube 실제 업로드 검증 (PC 필요)
- [ ] Anthropic 키 (결제 막힘 — 보류. 대본 파일로 대체 가능)
- [ ] X 또는 Threads 게시 검증

## 보류 리스트 (나중에)

> **PC 작업 가능 시간: 매일 오후 10시 이후.** 아래 PC 표시 항목은 그때 진행.

- **YouTube 업로드 설정 (PC 필요)** — 코드는 완성됨(`youtube.py`). Google Cloud에서 프로젝트 생성 → YouTube Data API v3 사용 설정 → OAuth 동의 화면(테스트 사용자에 본인 이메일 추가) → `client_secret.json` 받아 `axdata_13` 폴더에 넣기 → 첫 실행 시 브라우저 로그인. 폰에서는 인증·업로드가 안 되어 PC 전용.
- **Pexels 키 → `.env` (완료)** — 이미 넣어 사진 배경 동작 확인함.
- **배경음악 Jamendo 연동** — 코드는 완성됨(`video.py`의 `pick_music`). Gim이 `devportal.jamendo.com` 에서 `client_id` 발급 후 `.env`의 `JAMENDO_CLIENT_ID` 에 넣으면 자동 작동. 폴더(`music/`) 방식은 지금도 사용 가능. Jamendo 곡은 출처 표기(CC BY) 필요.
- **Anthropic 대본 자동 작성** — 결제 화면 문제로 보류. 대본 파일(`--file`)로 대체 중.
- **자막 넣기 (완료)** — 화면 가운데 흰 글씨+검은 테두리로 적용함.

## 결정과 근거

**Python 하나로 시작합니다.** Node를 같이 쓰면 실행·배포·의존성이 두 배로 복잡해집니다. 브라우저 자동화가 정말 필요해질 때 Node를 더합니다.

**공식 API만 씁니다.** 네이버 블로그·티스토리는 공식 API가 없어 브라우저 자동화가 필요한데, 약관 위반과 계정 정지 위험이 있어 제외했습니다.

**X부터 시작합니다.** 글만 있으면 되고 API가 가장 단순해서 "AI가 쓰고 → 올린다" 전체 흐름을 가장 빨리 검증할 수 있습니다.

**영상은 이미지 + 내레이션 슬라이드쇼입니다.** 실사 AI 영상 생성은 비싸고 API 접근이 까다로워, 무료 도구만으로 만들 수 있고 쇼츠에서 잘 통하는 형식을 골랐습니다.

**영상 도구는 전부 무료입니다.** 음성은 `edge-tts`(키 불필요), 이미지는 Pexels(무료 발급), 조립은 `imageio-ffmpeg`. ffmpeg을 시스템에 설치하지 않아도 되도록 Python 패키지로 받는 방식을 썼습니다.

**`imageio-ffmpeg`에는 `ffprobe`가 없습니다.** 영상 길이를 잴 때는 `ffmpeg -i` 출력에서 읽어야 합니다.

## 플랫폼 요금·제약 (2026-07-20 조사)

| 플랫폼 | 게시 요금 | 시작까지 | 하루 한도 | 걸림돌 |
|---|---|---|---|---|
| X | **유료** — 글 $0.015, 링크 글 $0.20 | 15분 | 크레딧만큼 | 선불 충전 필수 |
| Threads | 무료 | 3~5주 | 250개 | 사업자 인증 1주 + 권한별 심사 2~4주 |
| YouTube | 무료 | 1~2일 | 100개 | Google Cloud + OAuth 설정 |
| Instagram | 무료 | 3~5주 | 계정 규모에 따라 | 비즈니스 계정 + 페북 페이지 + 심사 |
| TikTok | 무료 | 심사 통과까지 | — | 심사 전엔 나만 보기로만 게시 |

핵심 교환 관계는 **"X는 돈이 들지만 빠르고, 나머지는 공짜지만 몇 주 걸린다"** 입니다.

### 조사 중 확인한 변경 사항

- **X 무료 등급은 2026-02-06 폐지**되었습니다. 신청 주소도 `developer.x.com` → **`console.x.com`** 으로 바뀌었고, 며칠 걸리던 심사는 없어졌습니다.
- **YouTube 업로드 비용이 2025-12-04 대폭 인하**되었습니다. `videos.insert` 가 약 1,600 → 약 100 유닛이 되어, 하루 상한이 6개에서 **100개**로 늘었습니다.

> 요금은 제3자 문서를 근거로 정리했습니다. 충전 전에 각 플랫폼 공식 페이지에서 실제 단가를 확인해야 합니다.
