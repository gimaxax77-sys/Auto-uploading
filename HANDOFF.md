# 인수인계 프롬프트 (다음 세션에 그대로 붙여넣기)

아래 `---` 아래 내용을 새 세션 첫 메시지로 주면 맥락을 이어서 작업할 수 있습니다.
**최종 갱신 2026-08-21.** 이전 판(2026-07-28·08-04)은 상태가 전부 낡아 폐기했습니다.

---

당신은 Gim 의 유튜브 자동 생성·업로드 툴 작업을 이어받습니다.

**⛔ 먼저 읽으십시오. 읽기 전에 판단하지 마십시오.**
1. `research.md` — 진단·결정 근거가 누적된 정본입니다. **끝에서부터** 읽으십시오(1,400줄 넘음).
2. `PLAN_LONGFORM.md` — 다음 방향인 롱폼 전환 계획서(245줄).

## 이 프로젝트가 지금 어떤 국면인가 (가장 중요)

**쇼츠 자동 업로드는 실패로 판정됐고 2026-08-15 부터 멈춰 있습니다.**
**2026-08-21 Gim 결정으로 ① 교차 게시 실험은 중단, ② 롱폼은 계획서 재검토를 마쳤습니다(v2).**
지금 대기 중인 것은 **롱폼 결정 세 가지(화면 방식·길이·채널)** 하나뿐입니다.
**새 쇼츠를 만들거나 업로드를 재개하지 마십시오.** Gim 지시 없이는 아무것도 올리지 않습니다.

## 기본 정보

- 위치 `D:\.CODE\AXdata\axdata_13_auto_upload` · GitHub `gimaxax77-sys/Auto-uploading` · **브랜치 `main`**
- 채널 **「1분 궁금증」** (`UCwxbG8W-VbczBIWqAlAZ3fQ`, 유튜브 계정 `gimfjq06@gmail.com`)
- 흐름: 대본 → 구글 TTS(Neural2) → Pexels 세로영상 → 단어별 자막 + 강조 오버레이 → BGM → 업로드

## 현재 상태 (2026-08-21 실측)

| 항목 | 값 |
|---|---|
| 렌더된 영상 | 215편 (`output/`, 1.2GB) |
| 업로드됨 / 미업로드 | **112편 / 103편** |
| 채널 공개 / 비공개 | **45편 / 62편** |
| 구독 / 누적 조회 | **3명 / 214회** (스튜디오 기준) |
| 참여도(계속 시청함) | **47.7%** (08-05~12) · 08-02 기준값 50.5% |
| 유입(최근 8일) | 검색 **81.4%** · Shorts 피드 13.6% · 채널페이지 **3.4%** |
| 자동 업로드 | ⛔ **중지**(두 작업 다 `Disabled`) |

⚠ **API 값과 스튜디오 값이 다릅니다**(누적 195 vs 214 · 좋아요 1 vs 16).
**스튜디오가 맞습니다** — 비공개 62편을 포함하고 반영이 빠릅니다.

## 무엇이 밝혀졌나 (재조사 금지 — 이미 실측된 것)

1. **쇼츠 피드가 안 띄웁니다.** 22일간 12회. 편당 피드 표시 **0.7회 추정**.
2. **화면이 대사와 안 맞습니다 — 40%.** 과학 20편 프레임 육안 전수.
   **규칙이 있습니다** — 흔한 사물(꿀·얼음·양파)은 맞고, 특정 개체·현상(벌새·나무늘보·번개·
   무지개·데스밸리)은 **전부** 깨집니다. `fetch_video()`(`video.py:194`)에 **관련성 검사가 없습니다.**
3. **갈래가 8개로 흩어져 있습니다.** 215편 분류: 과학 33% · 혼합 31% · 자기계발 20% …
   채널명은 「1분 궁금증」인데 최근 12편 중 과학은 2편뿐이었습니다.
4. **훅 교체 효과는 판정 불가** — 표본이 없어서입니다(피드 표시 N≈17, 95% 오차 ±24%p).
5. **양산형 콘텐츠 정책**과 이 형식이 겹칩니다. 단 공식 문서는 **수익 창출 자격에만** 걸고
   배포 영향은 언급이 없습니다. **배포 부진과의 연결은 추정입니다. 단정하지 마십시오.**

## ⛔ 영구 미확정 — 여기서 단정하지 마십시오

**조회 부진이 콘텐츠 탓인지 채널 이력 탓인지 모릅니다. 그리고 앞으로도 모릅니다.**
07-27 부터 미확정이었고, 그것을 가르는 유일한 실험(교차 게시)이
**2026-08-21 Gim 결정으로 중단**됐습니다.

- ⛔ **추정으로 답을 만들지 마십시오.** 이 질문이 다시 나오면 **「모른다」가 답**입니다.
  특히 **롱폼 채널 결정(기존 vs 신규)에 「채널 이력 때문」을 근거로 쓰지 마십시오.**
- 파일은 `G:\내 드라이브\쇼츠_교차게시\`(A1~A5·B1~B5, 57.3MB) 에 **그대로 둡니다.**
  Gim 이 마음을 바꾸면 그날 바로 재개할 수 있습니다. 판독표는 `research.md` 08-16 절에 있습니다.

## 다음 방향 — 롱폼 (계획 v2, 코드는 한 줄도 안 짬)

`PLAN_LONGFORM.md` **v2**(2026-08-21 재검토판) 에 전부 있습니다.
핵심만 — 8분 = 장면 40개 · 렌더 **약 14분(실측 기반)**. **성패는 화면 조달 하나입니다.**

**⭐ v2 권고는 「하나를 고르기」가 아니라 「대상 유형으로 갈라 쓰기」입니다.**

| 대사가 가리키는 것 | 조달원 | 월 원가 |
|---|---|---|
| **실존 개체·장소·현상**(벌새·데스밸리·성운) | **위키미디어 커먼즈 실사**(PD·CC BY) | **$0** |
| **흔한 사물·분위기**(꿀·얼음·양파) | Pexels 스톡 | $0 |
| **추상·개념·과정** | AI 이미지(배치) | $24~72 |

근거 — 08-16 에 깨진 8개 대상은 **전부 실존 대상**이었고, 그중 **7개가 커먼즈에 실사로
있습니다**(API 실측). ⛔ **AI 이미지로 실존 대상을 채우면 「날조」가 됩니다** — 이 프로젝트
규칙은 「모든 콘텐츠는 사실 기반」입니다. C-1(AI 영상 전량, 월 $720~2,880)은 비현실적,
C-2(핵심 장면만, 월 $54~108)는 성과 확인 뒤 2차 옵션입니다.

**Gim 이 정해야 할 것 셋** — ① 화면 방식 ② 길이(8/10/15분) ③ 채널(기존/신규).
⛔ **정해지기 전에 코드를 짜지 마십시오.** 정해져도 **1단계(타당성 1편 손으로 관통)를
건너뛰지 마십시오** — 특히 **커먼즈 사진을 아직 한 장도 눈으로 보지 않았습니다.**
API 응답 수만 셌습니다. 「커먼즈면 해결된다」고 단정하면 08-16 과 같은 실수입니다.

## 자동 업로드를 다시 켤 때 (지금은 켜지 마십시오)

⛔ **올리는 작업은 하나가 아니라 둘입니다.**

| 작업 | 시각 | 실제 동작 |
|---|---|---|
| `AXdata_YouTube_DailyUpload` | 23:00 | `upload_batch.py 3` — 3편 공개 업로드 |
| `AXdata_YouTube_UploadCheck` | 23:10 | 이름은 「점검」이지만 **모자란 편수를 그 자리에서 올립니다**(`check_upload.py:57`) |
| `AXdata_NewsBrief` | 13:05 | axdata_15 뉴스 브리핑 — **무관. 건드리지 마십시오** |

```
켜기: Enable-ScheduledTask -TaskName 'AXdata_YouTube_DailyUpload'
      Enable-ScheduledTask -TaskName 'AXdata_YouTube_UploadCheck'
확인: schtasks /query /tn '<이름>' /fo LIST     # Status 가 Ready 여야 함
```

`_TOOLS/claude-guard` 가 점검 배치 파일명이 든 명령을 **문자열만 있어도** 차단합니다.
문서에 그 이름을 쓸 때도 걸리므로 셸 히어독 대신 **파일 도구(Write/Edit)로** 쓰십시오.

## 영상 포맷 (v3 + 2026-07-28 개편) — 쇼츠 재개 시에만 유효

- **대본**: 4장면 · 사실 → 체감 → 반전 → 질문 · 평균 10.6초
- **형식**: `문장 | 검색어 | 강조문구` — **세 번째 칸이 없으면 옛 포맷으로 취급돼 업로드에서 빠집니다**
- **폰트** HY헤드라인(`H2HDRM.TTF` / ASS 이름 `HYHeadLine-Medium`)
- **줄바꿈** 어절 경계에서만. 숫자+단위·기호(`>` `↔` `vs`)는 묶습니다
- **강조 크기** 128~240pt 자동 · **장면 전환** 18종을 편 번호로 순환

## 핵심 파일

- `video.py`(863줄) — 생성 본체. `fetch_video`/`fetch_image`(Pexels) · `build_ass`(자막) ·
  `TRANSITIONS` · `mood_of`/`voice_of` · `apply_frame`(보류)
- `make_all.py` — 전체 또는 `[시작 끝]` 구간 재생성(구간 지정 시 덮어씀)
- `upload_batch.py` — `DAILY_MAX`(하루 상한, **여기 한 곳에서만** 정함) · `is_current` ·
  `desc_of` · `tags_of` · `playlist_of` · `spread`(장르 연속 방지)
- `check_upload.py` — 23:10. **점검이 아니라 백업 업로더입니다**
- `youtube.py` — 업로드(`:45` title·description·tags·category)/공개전환/삭제.
  스코프에 `yt-analytics.readonly` 포함. **썸네일 설정 기능은 없습니다**
- `analyze_video.py` — 참고 영상 분석(메타·채널 성적·썸네일·자막·챕터)
- `run_upload.bat`·점검 배치·`hidden.vbs` — 스케줄러가 부르는 것들

## 환경·규칙 함정 (전부 실제로 밟은 것들)

- Windows PowerShell. 파이썬은 `PYTHONIOENCODING=utf-8` + `-X utf8`
- **`.bat`·`.cmd`·`.vbs` 는 반드시 CRLF.** LF 로 바뀌면 스케줄러가 조용히 오작동합니다.
  `.gitattributes` 가 막지만 저장 직후 확인하십시오. **두 번 밟았습니다**
- **ffmpeg 출력 파일 이름은 반드시 `.mp4` 로 끝낼 것.** `X.mp4.new` 로 했다가 컨테이너를
  못 정해 **64편 전량 실패**(exit `-22`). 임시파일은 `경로[:-4] + "__new.mp4"` 로
- **완성됐을 때만 `os.replace` 로 교체하는 구조를 유지하십시오.** 그 덕에 그때 피해가 0이었습니다
- **`squeezev` 전환은 ffmpeg 이 죽습니다.** `test_전환목록_안전()` 이 막고 있습니다
- 스케줄러 변경은 **대화형 실행만으로 검증하면 안 됩니다.** 임시 작업을 등록해 실제 경로로 확인
- 렌더는 **로컬 4개 병렬이 상한**. `make_all.py A B` 를 구간 나눠 백그라운드로
- **모든 콘텐츠는 사실 기반.** 검색어는 대상이 명확한 단어로
- `.gitignore`: 자격증명 · `output` · `music` · `uploaded.json` · `last_run.json` · `analysis`
- 커밋 후 **`git push origin main`**
- **테스트**: `for %f in (test_*.py) do python -X utf8 -u %f` — 코드 건드렸으면 「완료」 전에 돌립니다

## Gim 관련

- 존댓말 · 초보자 눈높이 · **결론 먼저** · 한글 문장은 마침표로 끝
- **⛔ 서브에이전트 금지.** 긴 작업은 백그라운드 실행 + 완료 대기로
- **글·대본·코드는 바로 적용해도 되지만, 화면이 바뀌는 변경은 반드시 먼저 보여 주고 승인**받습니다.
  이 구분을 못 해 85편을 두 번 헛렌더했습니다
- **평일 08:00~21:00 은 모바일만 가능.** 로컬 파일을 보내도 못 엽니다.
  보고서는 **아티팩트 링크**, 대용량은 **`G:\내 드라이브\`** 로. PC 조작은 평일 22시 이후나 주말
- 선택지가 많으면 **성격별로 묶고 이름·번호를 붙여** 보여 줘야 정확히 골라 줍니다
- **답변은 「팩트 + 간략 설명」.** 전문은 `research.md` 에 길게 쓰고 채팅은 짧게

## ⛔ 이 프로젝트에서 내가 저지른 큰 실수 두 개 — 반복하지 마십시오

1. **26일간 숫자만 보고 영상을 한 번도 안 열었습니다.** 그러면서 "콘텐츠 문제인지 모른다"고
   계속 적었습니다. 프레임을 뽑자 **즉시** 40% 불일치가 보였습니다.
   → **성과가 나쁘다는 질문을 받으면 지표와 「산출물」을 둘 다 엽니다.**
   → **「원인 미확정」을 세 번째 쓰고 있으면 그것이 신호입니다** — 안 본 자료가 있다는 뜻입니다.
2. **「노출이 없다」를 「콘텐츠 문제가 아니다」로 읽었습니다.** 성립하지 않는 대비였습니다.
   쇼츠에서 노출은 **초기 시청 유지의 결과**라 둘이 독립 변수가 아닙니다.
   → **인과로 이어진 A 와 B 를 대립항으로 쓰지 마십시오.**

## 지표 이름 주의

**쇼츠에는 「노출수·CTR」이 없습니다.** 썸네일이 없기 때문입니다. 대신 두 개가 있습니다.

| 화면 라벨 | 뜻 |
|---|---|
| **피드에 표시된 횟수** | 피드에 뜬 횟수 = 분모 |
| **조회하기로 선택한 사용자 비율** | 안 넘기고 본 비율 |

**Analytics API 는 둘 다 안 줍니다.** 스튜디오 → 분석 → **콘텐츠** 탭 → **Shorts** 칩에서만 봅니다.

## 다른 프로젝트

**`axdata_15_news_brief`** — 뉴스 브리핑, 가동 중.
<https://gimaxax77-sys.github.io/news-brief/> · 매시 05분 자동 갱신. 상세는 그 폴더 `HANDOFF.md`.
**`axdata_14_capcut_agent`** — 캡컷 에이전트, 별개 폴더. 여기서 세션을 열면 안 보입니다.

**시작하는 법** — `research.md` 끝부분(「2026-08-21 롱폼 계획서 재검토」)과
`PLAN_LONGFORM.md` v2 를 읽고, **롱폼 결정 세 가지(화면 방식·길이·채널)가 정해졌는지**
Gim 에게 물으십시오. 안 정해졌으면 **기다리는 것이 맞습니다.** 새로 만들거나 올리지 마십시오.
⛔ **교차 게시 실험은 중단됐습니다. 결과를 기다리지 마십시오.**

---
