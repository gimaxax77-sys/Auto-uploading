# 인수인계 프롬프트 (다음 세션에 그대로 붙여넣기)

아래 `---` 아래 내용을 새 세션 첫 메시지로 주면, 맥락을 이어서 작업할 수 있습니다.

---

당신은 Gim의 YouTube 쇼츠 자동 생성·업로드 툴 작업을 이어받습니다.
먼저 저장소의 `research.md`를 읽으십시오. 진단 결과와 결정 근거가 거기에 누적돼 있습니다.

## 프로젝트

- 위치: `D:\.CODE\AXdata\axdata_13` (작업은 이 폴더에서)
- GitHub: `gimaxax77-sys/Auto-uploading`, 브랜치 `atup`
- 채널: **`1분 궁금증`** (유튜브 계정 `gimfjq06@gmail.com`)
- 흐름: 대본 → 구글 TTS(Neural2) → Pexels 세로영상 → 단어별 자막 + 강조 오버레이 → BGM → 자동 업로드

## 현재 상태 (2026-07-28 기준)

| 항목 | 값 |
|---|---|
| 제작한 영상 | 215편 |
| **업로드 대상(새 포맷)** | **85편** — 96~100 · 131~135 · 141~215 |
| 공개 | **3편** |
| 대기열 | **82편** (하루 3편이면 약 27일치) |
| 구독 / 누적 조회 | 2명 / 84회 |
| 나머지 130편 | 옛 갈래(동기부여·생활꿀팁·명소·반려·AI). **업로드 대상에서 제외됨** |

- 채널에 남은 비공개 58편은 전부 옛 갈래입니다. 손대지 않습니다.
- **자동 업로드**: 23:00 `AXdata_YouTube_DailyUpload` → 23:10 `AXdata_YouTube_UploadCheck`.
  둘 다 `hidden.vbs` 경유라 화면에 창이 안 뜹니다.
- **하루 3편 상한은 코드로 강제**됩니다(`upload_batch.DAILY_MAX`). 손으로 미리 올린 날에는
  밤에 자동으로 건너뜁니다. 편수는 이 상수 한 곳에서만 정하고 `check_upload` 가 가져다 씁니다.

## 영상 포맷 (v3 + 2026-07-28 개편)

- **대본**: 4장면 · **사실 → 체감 → 반전 → 질문** · 강조 문구는 1~3장면 · 평균 10.6초
- **형식**: `문장 | 검색어 | 강조문구`
  세 번째 칸이 없으면 **옛 포맷으로 취급되어 업로드 대상에서 빠집니다.**
- **폰트**: HY헤드라인(`H2HDRM.TTF` / ASS 이름 `HYHeadLine-Medium`)
- **줄바꿈**: 어절 경계에서만. 숫자+단위("만 원")와 기호(`>` `↔` `vs`)는 묶어서 안 갈라집니다.
- **강조 크기**: 128~240pt 자동. 두 줄 안에 들어가는 가장 큰 크기를 고릅니다.
- **장면 전환**: 18종을 편 번호로 돌려 씁니다. 확대량·전환 길이도 편마다 다릅니다.

## 이어서 할 일 / 대기

1. **매일 업로드 자동 진행 중** — 개입 불필요.
   확인은 `upload_log.txt` 마지막 줄이 `[점검 YYYY-MM-DD] 정상 완료.` 인지 보면 됩니다.
2. **새 콘텐츠** — 번호는 **216부터**. 대기열 82편이 약 27일 뒤 소진되므로 그 전에 보충합니다.
   반드시 v3 구조와 강조 문구를 넣어야 업로드 대상이 됩니다.
3. **수익화 — 보류(후순위)**. `research.md`의 「보류 — 수익화」 참고.
   **상태 점검 때 아래 두 수치를 같이 확인하고, 닿으면 그때 보고**합니다. 아직 멀면 언급하지 않습니다.
   - 주간 쇼츠 피드 유입 **1,000회** (현재 96편 통틀어 4회)
   - 구독 **500명** 또는 90일 쇼츠 유효조회 **150만**
4. **프레임 덧입히기 — 보류.** `research.md`의 「보류 — 프레임 덧입히기」에 되살리는 법이 있습니다.
5. **캡컷 에이전트**(별개 스펙) — Gim 결정 대기.

## 아직 못 푼 질문 (중요)

**조회 부진의 원인이 콘텐츠인지 채널 이력인지 모릅니다.** 포맷을 아무리 다듬어도 후자가
원인이면 결과가 안 바뀝니다. 가르는 방법은 **같은 영상을 이력 없는 새 채널(또는 틱톡·릴스)에
올려 보는 것**인데, Gim 지시로 보류 중입니다. 단정해서 말하지 마십시오.

## 핵심 파일

- `video.py` — 생성 본체. `어절_줄나눔`/`어절_묶기`/`강조_배치`(줄바꿈), `build_ass`,
  `TRANSITIONS`(전환), `apply_frame`(보류), `mood_of`/`voice_of`/`번호_of`.
- `make_all.py` — 전체 또는 `[시작 끝]` 구간 재생성(구간 지정 시 덮어씀).
- `upload_batch.py` — `DAILY_MAX`(하루 상한) · `is_current`(새 포맷만 대상) · `desc_of`(영상별
  소개글) · `tags_of` · `playlist_of`/`담기` · `spread`(장르 연속 방지).
- `check_upload.py` — 23:10 점검. `last_run.json`만 보고 판단합니다.
- `youtube.py` — 업로드/공개전환/삭제. 스코프에 `yt-analytics.readonly` 포함.
- `analyze_video.py` — 참고 영상 분석. 주소만 주면 메타·채널 성적·썸네일·자막·챕터를 뽑습니다.
- `run_upload.bat`·`check_upload.bat`·`hidden.vbs` — 스케줄러가 부르는 것들.

## 환경·규칙 주의

- Windows PowerShell. Python 명령엔 `PYTHONIOENCODING=utf-8` + `-X utf8`.
- **`.bat`·`.cmd`·`.vbs`는 반드시 CRLF.** 편집 도구가 LF로 바꾸면 스케줄러에서 조용히 오작동합니다.
  `.gitattributes`가 막지만, 저장 직후 `[IO.File]::ReadAllText`로 CRLF 수를 확인하십시오.
  **이 실수를 두 번 했습니다.**
- 스케줄러 관련 변경은 **대화형 실행만으로 검증하면 안 됩니다.** 임시 작업을 등록해 실제 경로로 돌려야 재현됩니다.
- **`squeezev` 전환은 ffmpeg 이 죽습니다.** `test_전환목록_안전()`이 막고 있습니다.
- 영상 렌더는 **로컬 4개 병렬이 상한**. `make_all.py A B`를 구간 나눠 백그라운드 실행.
- **모든 콘텐츠 사실 기반.** 검색어도 대상이 명확한 단어로(느슨하면 화성 편에 명왕성이 뜹니다).
- 커밋 후 `git push origin atup`. 자격증명·output·music·uploaded.json·last_run.json·analysis 는 `.gitignore`.
- 테스트: `for %f in (test_*.py) do python -X utf8 -u %f` — 코드 건드렸으면 "완료" 전에 돌립니다.

## Gim 관련

- 존댓말, 초보자 눈높이, 결론 먼저, 한글 문장은 마침표로 끝.
- **서브에이전트 금지.** 긴 작업은 백그라운드 실행 + 완료 대기로 처리합니다.
- **글·대본·코드는 바로 적용해도 되지만, 화면이 바뀌는 변경은 반드시 먼저 보여 주고 승인받습니다.**
  이 구분을 못 해 85편을 두 번 헛렌더했습니다.
- **평일 08:00~21:00 은 모바일만 가능.** 이 시간대에는 로컬 파일을 보내도 열지 못합니다.
  분석·보고서는 **아티팩트 링크**, 원문·대용량은 **`G:\내 드라이브\영상분석\`** 으로 보냅니다.
  PC 앞 조작은 평일 22시 이후나 주말에 안내합니다.
- 선택지가 많으면 **성격별로 묶고 이름·번호를 붙여** 보여 줘야 정확히 골라 줍니다.

먼저 `research.md`를 읽고, 위 대기 1~5와 `upload_log.txt` 마지막 줄을 확인한 뒤 Gim에게 상태를 브리핑하십시오.
