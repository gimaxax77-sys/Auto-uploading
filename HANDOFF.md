# 인수인계 프롬프트 (다음 세션에 그대로 붙여넣기)

아래 내용을 새 세션 첫 메시지로 주면, 맥락을 이어서 작업할 수 있습니다.

---

당신은 Gim의 SNS 자동 업로드 툴 작업을 이어받습니다. 아래 맥락을 먼저 파악하세요.

## 프로젝트

- 위치: `D:\.CODE\AXdata\axdata_13`  (작업은 이 폴더에서)
- GitHub: `gimaxax77-sys/Auto-uploading`, 작업 브랜치 `atup`
- 목적: 주제 대본 → AI 한국어 내레이션 → Pexels 사진 → 자막·효과 → 세로 쇼츠(mp4) → YouTube 공개 업로드까지 자동화.
- 상세 맥락은 저장소의 `research.md`, `PLAN.md` 를 반드시 먼저 읽을 것.

## 지금까지 완료

- 영상 생성 파이프라인 전체 실동작 확인(자막 배경밝기별 색 자동, 켄번즈·크로스페이드·비네팅·자막 페이드, 마지막 장면 음성 느리게 마무리).
- 대본 140편(`scripts/*.txt`, 한 줄=한 장면, `|` 뒤 영어 이미지 검색어).
- YouTube OAuth 설정 완료(`client_secret.json`, `token.json` 존재). **01~06편 공개 업로드 완료.**

## 진행 중/미완료 (이어서 할 일)

1. **140편 재생성 진행 중**(마무리 문구 개선판). 마지막 확인 128/140. 완료 여부 먼저 확인:
   `Get-ChildItem output\*.mp4 | Measure-Object` 가 140이면 완료.
   미완료면 `python make_all.py` 재실행(이미 만든 건 건너뜀).
2. **YouTube 쇼츠 탭 미표시 문제 조사 중**. 증상: 공개했는데 채널 "쇼츠" 탭에 안 뜸.
   - 가장 흔한 원인은 처리 지연(수 분~수 시간). 직접 링크·"동영상" 탭에는 뜨는지 먼저 확인.
   - 필요 시 제목 해시태그 소문자 `#shorts`→대문자 `#Shorts` 조정 후 재테스트.
   - 상세: `research.md` 의 "쇼츠 탭 미표시" 절.
3. **매일 업로드**: `python upload_batch.py 5` (하루 5편 공개, `uploaded.json`으로 중복 방지). 현재 6편 완료, 약 134편 남음. 하루 한도 약 100편.

## 핵심 파일

- `video.py` — 영상 생성 본체. 상단 상수(FONT/RATE/EFFECTS 등)로 자막·음성·효과 조절.
- `youtube.py` — 업로드(`upload`), 공개전환(`set_privacy`, 단 force-ssl 스코프 필요해 현재 토큰으론 불가).
- `upload_batch.py` — 하루 N편 공개 업로드.
- `make_all.py` — 대본 전체를 영상으로(이미 만든 건 건너뜀).
- `generate.py` — 대본 AI 생성(Anthropic 결제 막혀 현재 미사용, 대본 파일로 대체).

## 환경·규칙 주의

- Windows PowerShell. 한글 출력 깨지면 `$env:PYTHONIOENCODING="utf-8"` 먼저.
- Python 실행은 `axdata_13` 폴더 안에서(상대경로로 `client_secret.json` 등 참조).
- 이미지 검색어는 Pexels에 있는 안전한 단어로(없으면 업로드 실패). 특정 대상 사진이 없으면 오해 없는 중립 이미지 사용.
- **모든 콘텐츠는 사실 기반**. 추측·허구·오귀속 명언 금지. 사실성 콘텐츠는 검증 후 사용(필요 시 서브에이전트 병렬 검증).
- 자막 파일은 `newline="\n"`로 저장(윈도우 `\r\n`이면 줄 벌어짐).
- 커밋 후 `git push`. 영상·자격증명은 `.gitignore`로 제외됨(정상).

## Gim 관련

- 존댓말, 초보자 눈높이 설명, 결론 먼저.
- **직접 PC 조작(브라우저 로그인·파일 넣기 등)은 한국시간 오후 10시 이후만 가능.** 그 전엔 모바일. 단, 내가 명령으로 하는 영상 생성 등은 낮에도 가능.
- 모바일에선 첨부 파일이 안 열려 GitHub raw 주소로 결과 전달.
- Gim이 직접 해야 하는 일은 아주 상세히 단계별로 안내(URL·클릭 위치·성공 확인법·막히는 지점 경고).

먼저 `research.md`와 `PLAN.md`를 읽고, 위 미완료 1~3을 확인한 뒤 Gim에게 현재 상태를 브리핑하세요.
