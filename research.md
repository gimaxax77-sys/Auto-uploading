# 자동 업로드 툴 — 진행 기록 (research.md)

> 요청·진행 과정·결정과 근거·결과를 누적 기록. 다음 세션이 맥락을 다시 추론하지 않고 이어가도록.

## 현재 상태 (2026-07-28 기준)

- **채널**: `1분 궁금증` (계정 gimfjq06@gmail.com). 갈래는 **"왜 그럴까" 사실 다큐 하나**. 구독 2 · 누적 조회 84.
- **공개 영상 0편.** 채널에 남은 58편은 전부 비공개(옛 갈래). 7/28 23:00부터 새 포맷이 하루 3편씩 올라간다. 대기열 85편 = 약 28일치.
- **영상 215편 제작 완료.** 그중 **새 포맷(v3) 85편**만 업로드 대상(96~100 · 131~135 · 141~215). 나머지 130편(동기부여·생활꿀팁·명소·반려·AI·돈직장·감성)은 갈래에서 제외해 손대지 않는다.
- **v3 대본 구조**: 4장면 · **사실 → 체감 → 반전 → 질문** · 강조문구는 1~3장면 · 평균 11초. 형식은 `문장 | 검색어 | 강조문구`(세 번째 칸은 선택. 없으면 옛 포맷으로 취급되어 업로드 대상에서 빠진다).
- **파이프라인**: 대본 → 구글 TTS(Neural2) → Pexels 세로영상(없으면 사진) → 단어별 자막(ASS) + 중앙 강조 오버레이 → 무드별 BGM → 1080×1920.
- **업로드 자동화 2단계**(둘 다 `hidden.vbs` 경유라 창이 안 뜸). 23:00 업로드 3편 → 23:10 점검이 모자란 만큼만 보충(상한 3). 올릴 때 영상별 소개글·태그·카테고리 27(교육)·재생목록을 자동으로 붙인다.

## 핵심 파일

- `video.py` — 영상 생성 본체. 상단 상수로 조절. `build_ass`(단어자막 + 강조 오버레이), `mood_of`/`voice_of`.
- `make_all.py` — 전체 또는 `[시작 끝]` 구간 재생성(구간 지정 시 덮어씀).
- `upload_batch.py` — 하루 N편 공개 업로드. `is_current`(새 포맷만 대상) · `desc_of`(영상별 소개글) · `tags_of` · `playlist_of`/`담기` · `spread`(장르 연속 방지).
- `check_upload.py` — 23:10 점검. `last_run.json`만 보고 판단(로그 인코딩과 무관).
- `youtube.py` — 업로드/공개전환/삭제. 스코프: upload · force-ssl · **yt-analytics.readonly**.
- `analyze_video.py` — 참고 영상 분석(메타·채널 성적·썸네일·자동자막→문장·챕터 배분).
- `generate.py` — 글/대본 AI 생성(Anthropic 필요, 현재 결제 막혀 대본 파일로 대체).
- `scripts/` — 대본 215편. 한 줄=한 장면.

## 주요 결정과 근거

- **위인 명언(J) 분야 제외**: 링컨 명언 약 75% 가짜, 니체·아인슈타인 오귀속 다수(WebSearch 확인). 사실 기반 원칙과 충돌해 "세계 명소"로 교체.
- **사실 검증에 서브에이전트 활용**: 우주·자연 통념(별 반짝임=대기 굴절, 계절=기울기, 바다=물의 성질), 반려동물 오해, 명소 사실(빅벤=종 이름, 만리장성 21196km, 설악산=생물권보전지역) 등 병렬 검증.
- **마무리 문구**: "작은 습관이 ~합니다"류 반복이 어색 → 140편 마지막 줄을 질문형·여운형·행동유도형으로 다양화.

## 조회수 진단 (2026-07-27)

Gim이 "업로드 수 대비 조회수가 너무 저조하다"고 지적. 감으로 답하지 않고 API로 실측함.

- **실측**: 공개 96편 / **총 조회 84회** / 중앙값 **0회** / 최고 10회 / 구독 2명. 채널 개설 6일차.
- **기술적 결함 없음**(전수 확인): 쇼츠 분류 정상 · 1080×1920 18~23초 · 아동용 아님 · 지역제한 0 · 처리 완료.
- **분석 권한 확보**: `yt-analytics.readonly` 스코프 추가 + 클라우드 프로젝트에서 YouTube Analytics API 사용 설정.
  (노출수·클릭률은 API가 제공하지 않음. Studio 전용 지표.)
- **결정적 지표 — 유입 경로별 조회수**:

  | 경로 | 조회 |
  |---|---|
  | YT_CHANNEL(채널 페이지 직접) | 45 |
  | YT_SEARCH | 19 |
  | NO_LINK_OTHER | 8 |
  | EXT_URL | 6 |
  | SUBSCRIBER | 4 |
  | **SHORTS(쇼츠 피드)** | **4** |

  → **"보여줬는데 안 눌렀다"가 아니라 "애초에 안 보여주고 있다".**
  절반 이상이 채널 페이지 직접 방문이므로 외부 유입이 사실상 0.
- **시청 지속률은 판단 불가**: 조회 3~5회 표본에서 117~935%가 나옴(쇼츠 반복 재생). 통계로 의미 없음.
- **가설(미검증)**: 개설 6일 만에 96편 = 하루 16편. 대량 양산 신호일 수 있음. 근거는 정황뿐.
- **원인 미확정**: 콘텐츠 문제인지 채널 이력 문제인지 아직 구분 못 함. 같은 영상을 새 플랫폼(틱톡·릴스)에
  올려 보면 갈림. 이 검증은 보류 중.

### 조치 (2026-07-27~28)
- 공개 96편 → 한 갈래("왜 그럴까" 다큐)만 남기고 37편으로 정리 → 이후 전량 삭제하고 새 포맷으로 재업로드.
- 채널 정비: 이름 `1분 궁금증`, 소개·키워드·배너·프로필, 재생목록 6개, 카테고리 22→27(교육).
- 영상별 설명글·태그 자동 생성(`upload_batch.desc_of`/`tags_of`), 업로드 대상을 새 포맷 대본으로 제한.
- 업로드 속도 12편/일 → **3편/일**.

## 상태 점검 (2026-07-28 23:39)

코드 변경 없음. 실측만 함.

- **자동 업로드 정상.** `Get-ScheduledTask` 확인 — 두 작업 모두 State=Ready, 7/28 23:00·23:10 실행,
  LastTaskResult=0, 다음 실행 7/29 23:00·23:10 예약됨.
- **오늘 23:00 은 "건너뜀"이 정상 동작.** 낮에 손으로 3편을 올려 `last_run.json` 이
  `{"date":"2026-07-28","goal":3,"done":3}` 이므로 `DAILY_MAX` 상한에 걸려 건너뛴 것.
- **대기열 실측**(`upload_batch.is_current` + `uploaded.json` 으로 계산):
  output 215편 → 새 포맷 85편 → 업로드 완료 3편(`158` `193` `211`) → **남은 82편**.
  하루 3편이면 **2026-08-25 무렵 소진**. 그 전에 216번부터 보충 필요.
- **7/27 은 로그 줄이 아예 없다 = 그날 스케줄이 안 돌았다.** 실패가 아니라 미실행(실패였다면
  `exit=` 줄이 남는다). PC 전원이 꺼져 있었을 가능성이 크지만 **확인 못 했다**(작업 기록은
  마지막 실행만 남고 이력이 없음). 대기열이 하루 밀릴 뿐 데이터 손상은 없다.
- **수익화 재검토 조건 — 아직 멀다.** 구독 2명(기준 500) · 최근 7일 `SHORTS` 유입 4회(기준 1,000).
  이 수치는 삭제 전 옛 영상이 남긴 채널 누적치이며 새 포맷 3편의 성적이 아니다.

## 참고 채널 분석

### 혼잡스 「클로드로 당장 수익 만드는 확실한 방법 5가지」 (2026-07-28 분석)
- 8분 5초 롱폼 · 조회 151,767 · 좋아요 **4.3%** · 댓글 0.07% · 태그 없음 · 분당 561자.
- 채널 구독 81,200 / 61편 / 편당 평균 37,055회. 최근 12편 중앙값 28,472 → **평소의 5배지만 이상치 아님**.
- **도입 15초 = 질문 → 내가 치른 대가(500만 원) → 검증 범위 약속 → 통념 깨기**.
  정보가 아니라 *정보를 걸러 준 사람의 자격*을 먼저 판다.
- 도입 30초 안에 완주 장치 3개(뒤가 더 좋다는 예고 · 끝까지 보라는 직접 요청 · 댓글 유도).
  **약속대로 마지막 챕터에 가장 긴 106초를 쓴다.**
- 본문 5챕터가 전부 같은 5칸: 선언 → **증거 먼저** → 왜 팔리는가 → 실행 절차 → 꿀팁/함정.
- 마무리 4단: 요약 → 공통점 → 시청자 몫 지정 → **기한 붙은 행동 지시**("이번 주에").
- **옮길 수 있는 것**: 증거 선행(v3 반영됨) · 자문자답 · 부정형 훅 · 마지막을 질문 대신 행동 지시로.
- **옮길 수 없는 것**: 8분 5단 구조 / **화자의 대가(AI 내레이션엔 자격이 없음)** / **시청자 이득(저쪽은 돈,
  우리는 지식 한 조각)**. 뒤 두 가지가 진짜 격차.
- 자료: `analysis/qEVZ7AgB7zI_*` (자막 전문·썸네일·노트), 웹 노트 아티팩트.
- **한계**: 영상을 보지 못함. 화면·편집·음악·말투는 분석에 없음. 자막·썸네일·메타데이터만으로 쓴 것.

## 미해결·조사 필요

### ~~YouTube 쇼츠 탭에 업로드 영상이 안 보이는 문제~~ (해결됨, 2026-07-27)
**결론: 문제 없었음.** `youtube.com/shorts/<id>` 가 리다이렉트 없이 열리는지로 12편을 전수 확인한 결과
**12/12 전부 쇼츠로 정상 분류**. 제목의 `#shorts` 표기도 불필요해 제거함(세로 60초 이하면 자동 분류).
아래는 당시 조사 기록으로만 남김.

<details><summary>2026-07-22 당시 조사 내용</summary>
- 증상: 01~06 공개 업로드했으나 채널 새로고침해도 "쇼츠" 탭에 안 나타남.
- 조사 결과(WebSearch):
  1. **처리 지연** — 업로드 후 쇼츠 분류·노출까지 수 분~수 시간 걸릴 수 있음(정상).
  2. **쇼츠 분류 기준** — 9:16 세로 + 60초 이하. 우리 영상은 1080×1920·15~24초로 충족.
  3. **API 업로드 신호** — 제목/설명에 `#Shorts` 포함이 가장 확실한 신호. 현재 제목에 `#shorts` 넣고 있음(소문자). 대문자 `#Shorts` 권장 여부 재확인 필요.
  4. 세로·짧아도 플래그가 약하면 "일반 동영상"으로 게시되어 쇼츠 피드에 안 뜰 수 있음.
- **다음 확인 사항**:
  - 직접 링크(youtu.be/…)로는 재생되는지, "동영상" 탭에는 뜨는지 확인.
  - 몇 시간 뒤 쇼츠 탭 재확인(처리 지연 가능성).
  - 필요 시 제목/설명의 해시태그를 `#Shorts`(대문자)로 조정 후 재업로드 테스트.
  - 참고: [GitHub api-samples #484 — 쇼츠로 분류 안 되는 문제](https://github.com/youtube/api-samples/issues/484), [Postproxy YouTube Upload API 가이드](https://postproxy.dev/blog/youtube-upload-api-guide/)
</details>

## 참고 사항 (해결된 것 포함)

- **OAuth**: 동의 화면 "게시" 전환 완료(토큰 만료 없음). 스코프는 upload · force-ssl · yt-analytics.readonly 세 개이며 재인증 완료(2026-07-27). 클라우드 프로젝트 `auto-upload-503117`에서 YouTube Analytics API 사용 설정도 켜 둠.
- **업로드 quota**: `videos.insert` 는 2025-12-04 약 100유닛으로 인하, 2026-06-01 전용 한도로 분리. 하루 3편은 여유가 많다. 하루 한도는 약 100편.
- **분석 API 한계**: 노출수(impressions)·클릭률은 YouTube Analytics API가 제공하지 않는다(400). Studio 화면 전용. 대신 `insightTrafficSourceType`(유입 경로별 조회수)이 사실상 같은 판단을 준다.
- **남의 영상**: 파일은 내려받지 않는다(유튜브 약관). 자동자막·썸네일·메타데이터까지가 분석 한계.
- (보류) Anthropic 결제 미해결, Jamendo 키 미발급.

## 보류 — 프레임 덧입히기 (2026-07-28, Gim 지시로 이번 수정안에서 제외)

배경을 통째로 바꾸지 않고 **실사 위에 테두리·무늬를 얹는** 방식. 만들어 확인까지 마쳤으나
이번 개편에는 넣지 않는다. 코드는 남아 있고 대본에 표시가 없어 동작하지 않는다.

- **되살리는 법**: 대본 검색어 앞에 `@frame ` 을 붙인다(예: `@frame mars red planet`).
  그 장면은 B-roll 대신 사진을 받고 `video.apply_frame` 이 테두리·무늬를 얹으며,
  켄번즈 확대는 자동으로 꺼진다(`render_clip(still=True)`).
- **구성**: 이중 테두리(바깥 6px·안쪽 2px) + 네 모서리 강조(14px) + 전면 무늬(알파 72) +
  상하단 그늘. 무늬는 6종(`video.FRAME_PATTERNS`).
- **톤 맞추기**: `video.사진_톤()` 이 사진을 48×48로 줄여 **채도 있는 화소만** 골라 대표 색상을
  뽑고, 그 색상은 유지한 채 채도를 낮추고 밝기를 올려 얹는다. 그래서 편마다 색이 다르다.
- **직접 겪은 함정 두 가지**
  1. 켄번즈 확대가 켜져 있으면 **테두리가 화면 밖으로 잘려 안 보인다.** 확대를 꺼야 한다.
  2. 상하단 그늘을 150 으로 잡으니 사진이 안 보였다. 112 로 낮춰야 균형이 맞았다.
- **먼저 시도했다가 버린 것**: 배경을 통째로 교체하는 "카드" 방식(팔레트 6종 × 무늬 4종).
  팔레트를 `씨앗%6`, 무늬를 `씨앗%4` 로 뽑아 **6과 4가 약수 2를 공유해 12가지로 묶였고**,
  실제 쓰인 것은 8가지뿐이었다(팔레트 2종은 한 번도 안 나옴). 서로 나누어떨어지지 않는
  수를 쓰거나 축을 분리해야 한다.

## 화면 관련 확정 사항 (2026-07-28)

- **폰트**: 맑은 고딕 굵게 → **HY헤드라인**(`H2HDRM.TTF`, ASS 이름 `HYHeadLine-Medium`).
  폭이 좁아 한 줄에 더 들어간다.
- **줄바꿈**: libass 는 한글을 CJK 로 보아 **어절 한가운데서도 줄을 끊는다**("같은 만 / 원인데").
  ASS `WrapStyle` 을 2(자동 줄바꿈 없음)로 두고 `video.어절_줄나눔` 이 어절 경계에서만 `\N` 을 넣는다.
  `video.어절_묶기` 는 **숫자+단위**(만 원, 2,600 km)와 **기호**(>, ↔, vs)를 한 덩어리로 묶어
  갈라지지 않게 한다.
- **강조 문구 크기**: 240pt 로는 한글이 한 줄에 서너 자뿐이라 문구 255개 중 **239개가 3~4줄로
  쪼개졌다.** `video.강조_배치` 가 두 줄 안에 들어가는 가장 큰 크기를 128~240pt 에서 고른다.
  줄 수만 보고 폭을 안 보면 묶은 덩어리가 화면 밖으로 삐져나가므로 **둘 다** 확인해야 한다.
- **장면 전환**: 전편 `fade` 하나였던 것을 **18종**으로 늘려 편 번호로 돌려 쓴다(`video.TRANSITIONS`).
  - ⚠️ **`squeezev` 는 이 ffmpeg 빌드에서 해상도와 무관하게 접근 위반(0xC0000005)으로 죽는다.**
    절대 목록에 넣지 말 것. 넣으면 그 편의 렌더가 통째로 실패한다.
  - 전체 목록은 58종이며 `ffmpeg -h filter=xfade` 로 확인할 수 있다.

## 보류 — 수익화 (2026-07-28 검토, Gim 지시로 후순위)

제품 홍보·설명 영상 수익화를 검토했다. **네 갈래인데 셋은 조회수 병목에 걸려 지금 0원이다.**

| 모델 | 수익 조건 | 2026-07-28 상태 |
|---|---|---|
| A 애드센스 | 구독 500 + 90일 쇼츠 유효조회 **300만** | 구독 2 · 누적 84회 → 3만 5천 배 부족 |
| B 제휴 마케팅(쿠팡 파트너스 등) | 내 영상 시청자가 클릭·구매 | 조회 없으면 클릭 0 |
| C 브랜드 협찬 | 광고주가 채널을 사감 | 구독 2명엔 안 붙음 |
| D 제작 대행(B2B 외주) | 의뢰인이 지불. **채널 무관** | 기술은 지금도 가능. Gim 지시로 함께 보류 |

- **판단**: A·B·C는 전부 "조회수"라는 같은 병목. 지금 제휴 링크를 붙이면 겨우 하나로 좁힌
  채널 정체성만 흐려지고 수익은 0이다. **손대지 않는다.**
- **D 관련 사실**: 크몽 숏폼 제작 기본 2만 원~, 평균 13만 원, 납기 3일 이상. 우리 파이프라인은
  몇 분. 다만 ① 의뢰인 제품 소재가 필수(스톡으로 특정 제품 불가) ② 리뷰 0에서 시작 ③ 5천 원짜리
  경쟁 존재. **채널 사업이 아니라 외주 제작업이라는 점이 핵심 차이.**
- **B를 언젠가 할 때 법적 의무**: 공정위 「추천·보증 등에 관한 표시·광고 심사지침」. 쇼츠는 업로드
  설정의 **"유료 프로모션 포함"** 체크로 화면 표시. 설명란·고정댓글 링크는 **링크 바로 옆에** 대가성
  표기("더보기"를 눌러야 보이면 위반).

### 재검토 조건 (아래 중 하나라도 닿으면 Gim에게 다시 보고할 것)

1. **포맷 검증 신호** — 주간 쇼츠 피드 유입이 **1,000회** 돌파. (지금 96편 통틀어 4회)
   확인법: `insightTrafficSourceType` 조회에서 `SHORTS` 값.
2. **수익화 사정권** — **구독 500명** 또는 **90일 쇼츠 유효조회 300만**의 절반(150만) 도달.
3. Gim이 먼저 물어볼 때.

**상태 점검 때마다 1·2번 수치를 같이 확인할 것.** 아직 멀었으면 언급하지 않는다.

## Gim 시간대 주의

평일 08:00~21:00 은 **모바일만** 가능하다. 이 시간대에는 로컬 파일을 보내도 열지 못한다.
- 분석·보고서류 → **웹 아티팩트 링크**로 전달
- 원문·대용량·저작권 있는 자료 → **`G:\내 드라이브\영상분석\`** 에 복사(폰 드라이브 앱에서 열림)
- PC 앞 조작(설치·로그인·Studio 설정)은 평일 22시 이후 또는 주말에 안내

---

## 상태 점검 — 2026-07-28(화) 23:40

**요청**: "상태점검". 코드 변경 없음. 조회·확인만 수행.

### 확인 방법

`git status`/`git log` · `output/*.mp4` 개수 · `uploaded.json`+`upload_batch.is_current` 로 대기열 재계산 ·
`upload_log.txt`(UTF-8 로 다시 읽음) · `last_run.json` · `Get-ScheduledTaskInfo` · 시스템 이벤트 로그.

### 사실

| 항목 | 값 | 근거 |
|---|---|---|
| 렌더 완료 | 215편 | `output/*.mp4` 개수 |
| 새 포맷(업로드 대상) | 85편 | `is_current()` 로 전수 판정 |
| 공개됨 | 3편 — 158·193·211 | `uploaded.json` ∩ 새 포맷 |
| 대기열 | 82편 (하루 3편 → 약 27일) | 위 둘의 차 |
| 오늘(07-28) | 3/3 완료 (21:50 수동분) | `last_run.json` = `{date:2026-07-28, goal:3, done:3}` |
| 23:00 자동 업로드 | 정상 — 상한 도달로 건너뜀 | 로그 "오늘 이미 3편…건너뜁니다", exit=0 |
| 23:10 점검 | 정상 완료, 추가 없음 | 로그 exit=0 |
| 스케줄러 | 둘 다 Ready, 다음 07-29 23:00/23:10 | `Get-ScheduledTaskInfo` |
| git | `atup` = `origin/atup`, 작업트리 깨끗 | `git status -sb` |
| 수익화 지표 | 공개 3편·누적 84회. 주간 1,000회 기준에 한참 못 미침 | 보고 조건 미달 |

**로그 읽는 법 정정** — `upload_log.txt` 는 실행 **출력이 먼저**, `[시각] exit=0` 헤더가 **나중에**
붙는다. 헤더 아래 글을 그 실행의 결과로 읽으면 하루씩 밀려 오독한다.

### 발견한 문제 — 07-27 하루가 통째로 비었다

- `upload_log.txt` 에 **2026-07-27 항목이 하나도 없다.** 07-26 다음이 바로 07-28 23:00 이다.
- 즉 그날 `run_upload.bat` 이 **아예 실행되지 않았다**(실행됐다면 실패해도 `exit=N` 헤더는 남는다).
- 결과 피해는 **업로드 3편 밀림**뿐. 데이터 손상·중복 없음. 대기열이 하루 늘었다.

**원인은 확인하지 못했다.** 이유:

- 작업 스케줄러 기록(History)이 **꺼져 있어** 해당 시각 실행 이력이 없다.
- `Get-ScheduledTaskInfo` 는 **마지막 실행 1건**만 보여 준다(07-28 23:00, 결과 0).
- 배제된 가설: 재부팅(마지막 부팅 07-25 12:42, 그 뒤 무재부팅) · 배터리(데스크톱이라 무관).
- 남은 가설(미검증): 07-27 23:00 에 PC 절전 상태였고, `WakeToRun=False` 라 깨우지 못함.
  `StartWhenAvailable=True` 지만 이후 실행 흔적이 없어 이 가설도 확정 못 한다.
- `check_upload.py` 는 **당일 날짜만** 보므로 지나간 날의 누락은 원리적으로 못 메운다(설계대로).

### 권고

1. (우선) **작업 스케줄러 기록 켜기** — 관리자 PowerShell 에서
   `wevtutil sl Microsoft-Windows-TaskScheduler/Operational /e:true`.
   다음에 또 비면 그때는 원인을 특정할 수 있다. 지금은 추측만 가능하다.
2. 절전이 원인일 경우의 대비는 `WakeToRun=True` 한 줄이지만, **원인 확정 전에는 손대지 않는다**.
3. 대기열 82편은 약 27일치. 216번부터의 신규 대본은 그 전에 보충하면 된다. 지금은 급하지 않다.

---

## 2026-07-30(목) 01:17 — 업로드 상태 점검

**요청** — "업로드 상태 체크". 코드 변경 없음, 조회만.

**확인한 것과 근거**

| 항목 | 결과 | 확인 수단 |
|---|---|---|
| 07-29 23:00 업로드 | **정상 3편** (`첫 정보에 휘둘리는 이유` / `뒤로 나는 유일한 새` / `우유가 치즈가 되는 법`) | `upload_log.txt` exit=0, 각 편 재생목록 편입까지 기록 |
| 07-29 23:10 점검 | 정상 완료, 추가 업로드 없음 | 같은 로그 `check exit=0` |
| 하루 상한 | 3편, 당일 소진 | `last_run.json` = `{"date":"2026-07-29","goal":3,"done":3}` |
| 누적 진행 | **64 / 215편** (남은 대기열 151편) | `uploaded.json` 64건 · `output/*.mp4` 215개 · `scripts/` 215개 |
| 스케줄러 | 둘 다 Ready. 다음 **07-30 23:00**(업로드) / **23:10**(점검) | `schtasks /query /fo CSV` |
| 오늘(07-30) 실행 | 아직 없음 — 예정 시각 전 | 현재 01:17 |
| git | `atup` 브랜치에 미커밋 5개(문서 4 + `.bat` 경로 수정 2) | `git diff --stat` |

**`.bat` 경로 수정은 실제 반영돼 있다** — 작업트리의 `run_upload.bat` · `check_upload.bat` 은
`axdata_13` → `axdata_13_auto_upload` 로 고쳐진 상태이고, 스케줄러는 이 파일을 그대로 호출한다.
커밋만 안 됐을 뿐 동작에는 문제 없다(07-28·07-29 연속 성공이 그 증거).

**미해결 — 07-27 공백의 원인은 여전히 모른다**

- 권고 1번(스케줄러 기록 켜기)이 **아직 적용되지 않았다**. `wevtutil gl Microsoft-Windows-TaskScheduler/Operational` → `enabled: false`.
- 켜지 않으면 다음에 또 비어도 원인을 특정할 수 없다. 관리자 권한 PowerShell 이 필요해 Gim 이 직접 눌러야 한다.

**결론** — 파이프라인 정상. 07-28·07-29 연속 정상 실행. 조치할 것은 스케줄러 기록 켜기 하나.

**후속(같은 날 01:2x) — 스케줄러 기록 켜기 완료.** Gim 이 관리자 PowerShell 에서 실행.
처음 안내한 `wevtutil sm ...` 은 **오타**였고(`sm` 명령 없음), 올바른 verb 는 **`sl`**(set-log) 이다.

```
wevtutil sl Microsoft-Windows-TaskScheduler/Operational /e:true
wevtutil gl Microsoft-Windows-TaskScheduler/Operational   → enabled: true
```

로그 파일 `%SystemRoot%\System32\Winevt\Logs\Microsoft-Windows-TaskScheduler%4Operational.evtx`,
최대 10MB, retention=false(가득 차면 오래된 것부터 덮어씀). 07-27 같은 공백이 재발하면
`Get-WinEvent -LogName Microsoft-Windows-TaskScheduler/Operational` 으로 그 시각을 추적할 수 있다.
**단, 소급 적용은 안 된다** — 07-27 공백의 원인은 앞으로도 확정 불가.

---

## 2026-08-01(토) 23:5x — "이틀째 알림이 없다" 조사 + 텔레그램 알림 구현

**요청** — "왜 이틀이나 지났는데 영상 업로드 알림을 안 띄워?"

### 조사 결과 — 업로드는 전부 정상. 알림 기능이 처음부터 없었다

| 날짜 | 결과 | 누적 |
|---|---|---|
| 07-30 23:00 | 3편 정상 | 67 |
| 07-31 23:00 | 3편 정상 | 70 |
| 08-01 23:00 | 3편 정상 | 73 |

`Get-ScheduledTaskInfo` → 두 작업 모두 `LastTaskResult: 0`, `NumberOfMissedRuns: 0`.
프로젝트 전체를 `알림|notify|telegram|smtp|메일` 로 검색했으나 **전송 코드가 한 줄도 없었다.**
결과는 `upload_log.txt` 파일에만 쌓였고, 클로드는 세션이 열릴 때만 동작하므로 스스로 띄울 수 없다.
**고장이 아니라 미구현이었다.**

### 부수 확인 — `남은 미업로드` 숫자의 정체

`upload_batch.py` 의 `is_current()` 가 **강조문구가 있는 대본(새 채널 갈래)만** 대상으로 잡는다.
그래서 `output/*.mp4` 215편 중 옛 갈래(동기부여·생활꿀팁 등, 비공개로 내린 것)는 제외된다.
현재 갈래 143편 = 업로드 73 + 대기 70. **대기 70편 ÷ 3편/일 ≈ 23일**(08-24 경 소진).
버그가 아니라 설계대로다. 다만 로그의 `누적 업로드: 73 / 215편` 표기는 분모가 전체 파일 수라 오해를 부른다.

### 구현 — 붙인 위치를 Gim 선택안에서 바꿨다

Gim 선택은 "`upload_batch.py` 끝에 전송 한 줄" 이었으나 **`check_upload.py`(23:10 점검)에 넣었다.**
이유: `upload_batch.py` 가 중간에 죽으면(네트워크·할당량) 마지막 줄에 도달하지 못해
**정작 알아야 할 실패가 조용히 넘어간다.** `check_upload.py` 는 10분 뒤 독립적으로 돌면서
`last_run.json` 상태를 읽으므로 성공·부분실패·전량실패를 모두 보고할 수 있다.
같은 값(더 나은 값)에 코드량은 오히려 적다.

**변경 파일 4개**

- `upload_batch.py` — 업로드 대상 판정을 `pending()` 함수로 추출. `main()` 과 알림이 같은 함수를 쓴다
  (같은 필터를 두 곳에 복사하면 7/26 `DAILY_MAX` 사고처럼 어긋난다).
- `check_upload.py` — `notify()` 추가. `.env` 의 `TELEGRAM_BOT_TOKEN`·`TELEGRAM_CHAT_ID` 가
  **둘 다 있을 때만** 전송하고, 전송이 터져도 `except` 로 삼켜 점검 자체는 정상 종료시킨다.
  결과는 3갈래 — 정상 완료 / 누락→재시도 복구 / 재시도 후에도 실패.
- `test_check_upload.py` — 알림 테스트 5개 추가(7/7 → 12/12). `requests.post` 를 가짜로 갈아끼워
  **망 접속 없이** 검증. 키 없음·부분 키·주소·본문·예외 삼킴을 각각 확인.
- `.env.example` — 키 두 개와 발급 방법 주석. 실제 `.env` 에는 빈 값으로만 넣어 뒀다(gitignore 확인함).

**검증**

```
Get-ChildItem test_*.py | ForEach-Object { python -X utf8 -u $_.Name }   → 5개 파일 전부 통과(12/12 포함)
C:\...\Python314\python.exe -X utf8 -c "import requests, dotenv, check_upload"  → import OK
```

두 번째 명령이 중요하다. **스케줄러가 부르는 파이썬은 PATH 의 python 과 다를 수 있고**, 다르면
새로 추가한 `requests`·`dotenv` import 가 밤 11시10분에 터져 점검이 통째로 죽는다.
실제로는 같은 실행 파일이었고 `pending()` 도 70편으로 정상 계산됐다.

### 남은 것 — Gim 이 봇 토큰을 발급해야 동작한다

키가 비어 있으면 `notify()` 는 조용히 아무 일도 안 한다(기존 동작 그대로, 위험 없음).
첫 알림은 토큰을 넣은 다음 날 **23:10** 에 온다.

**이 방식이 못 잡는 것** — 07-27 처럼 **스케줄러가 아예 안 뜨는** 경우엔 `check_upload` 도 안 돌아
알림도 안 온다. Gim 에게 리스크로 미리 고지했고 선택을 유지했다. 이걸 잡으려면 PC 밖(클라우드·폰)에서
도는 감시가 필요하다.

### 설정 중 실제로 걸린 함정 — `update_id` 를 `chat.id` 로 착각

첫 전송이 `HTTP 400 Bad Request: chat not found` 로 실패했다. 원인은 Gim 이 `getUpdates` 응답에서
**`update_id`(981153384)** 를 복사한 것이었다. 진짜 값은 `chat.id`(7403671803).

- `update_id` 가 JSON **맨 앞**에 나오고 자릿수도 9자리로 비슷해 눈으로 구분이 안 된다.
- 내 안내가 `"chat":{"id":...` 를 찾으라고만 했지 **바로 위의 update_id 를 조심하라고 짚지 않았다.**
- 다음부터는 안내 대신 **`getUpdates` 를 내가 호출해 chat.id 를 뽑아 주는 것이 확실하다**
  (`getMe` 로 봇 이름까지 함께 확인되어 토큰 오타도 같이 걸러진다).

교정 후 재전송 → `ok: true`, `message_id 7`, 받는 사람 `Gimss` 확인. **연결 완료.**

부수 사고 하나 더 — 확인 코드에서 응답을 `r.text[:300]` 으로 잘라 `json.loads` 가
`Unterminated string` 으로 터졌다. 성공 응답은 message 객체가 붙어 300자를 넘는다.
**응답을 자른 뒤 파싱하지 않는다.** `r.json()` 을 그대로 쓴다.

---

## 2026-08-02(일) 00:0x — 유튜브 채널 `1분 궁금증` 현황 전수 실측

**요청** — "현재 유튜브 채널 상황 점검해서 브리핑". 코드 변경 없음.
**방법** — YouTube Data API v3 + YouTube Analytics API v2 로 전수 조회(`token.json` 의 analytics 권한 사용).
로컬 `output/*.mp4` 는 ffmpeg 로 길이·해상도 실측. 조사 스크립트는 1회용이라 scratchpad 에 두었다.

### 1. 채널 개요 (실측)

| 항목 | 값 |
|---|---|
| 개설일 | 2026-07-21 (**12일차**) |
| 구독자 | **3명** |
| 채널 총 조회 | 33회 (채널 통계값) |
| 업로드 전량 | 73편 |
| 공개 | **15편** / 비공개 58편 |

### 2. 비공개 58편은 정상이다 — 확인함

날짜 분포가 07-21~07-26 에만 몰려 있고 제목이 전부 `#shorts` 가 붙은 옛 갈래
(동기부여·여행·생활꿀팁)다. **Gim 이 의도적으로 내린 것**이고 `is_current()` 가 재업로드를 막고 있다.
`uploadStatus` 는 73편 전부 `processed`, `rejectionReason` 전무 → **유튜브가 막은 것이 아니다.**
58 + 15 = 73 으로 `uploaded.json` 과도 일치한다.

### 3. 공개 15편 성적 (실측)

- 조회수 합계 **44회** · 편당 평균 2.9회 · **중앙값 0회**
- 상위: 뒤로 나는 유일한 새 12회 · 우주는 대부분 텅 비어 있다 8회 · 번개는 태양보다 뜨겁다 7회
- **07-30 이후 공개한 9편 중 7편이 0회**
- 좋아요는 15편 통틀어 1개, 댓글 0

### 4. 유입 경로 — 알고리즘이 거의 태우지 않는다

| 경로 | 조회 | 비중 |
|---|---|---|
| YT_CHANNEL (채널 페이지 직접) | 61 | **51.3%** |
| YT_SEARCH (검색) | 33 | 27.7% |
| SHORTS (쇼츠 피드) | **6** | **5.0%** |
| 기타 | 19 | 16.0% |

절반이 채널 페이지 방문이다. **쇼츠 피드 유입 6회**는 사실상 없는 것과 같다.

### 5. 새 갈래 영상은 옛 갈래의 절반 길이다 (실측)

| | 길이 | 대본 | 글자 수 |
|---|---|---|---|
| 새 갈래(85편) | **9.6~11.7초** | 4문장 | 74~93자 |
| 옛 갈래(130편) | 22.0~23.8초 | 5문장 | 125~152자 |

기술 규격은 정상이다 — `1080x1920`(9:16), 60초 미만, 설명에 `#Shorts`.
**쇼츠로 인식되지 않을 이유는 없다.**

시청 지속률도 나쁘지 않다(새 갈래 63~110%, 100% 초과는 루프 재생).
다만 **절대 시청시간이 옛 갈래의 절반**이다(약 9~12초 vs 약 20초).

### 6. ⚠ 확인하지 못한 것 — 노출수(impressions)·클릭률(CTR)

`metrics=impressions,impressionClickThroughRate` 요청이 **HTTP 400 으로 거부**됐다.
이 두 지표는 **YouTube Analytics API 가 지원하지 않고 YouTube Studio 에서만** 볼 수 있다.

→ **`youtube.py:14` 의 주석이 틀렸다.** `yt-analytics.readonly` 스코프를 두고
"노출수·클릭률 조회에 필요"라고 적혀 있으나 그 두 지표는 이 API 로 못 가져온다.
(스코프 자체는 조회수·시청지속률 조회에 실제로 필요하므로 스코프는 그대로 두면 된다.)

**이게 왜 결정적인가** — 노출수를 봐야 원인이 갈린다.
- 노출 자체가 적다 → 알고리즘 테스트 미통과. 콘텐츠·초기 반응 문제.
- 노출은 많은데 CTR 이 낮다 → 첫 프레임·제목 문제.

**처방이 정반대**라 노출수 없이 개선안을 내면 추측이 된다.
Gim 이 YouTube Studio 앱에서 5분이면 확인할 수 있다.

또 하나 못 본 것 — `dimensions=video,insightTrafficSourceType` 조합도 400 으로 거부됐다.
**영상별 유입 경로**는 API 로 확인 불가.

### 7. 데이터 지연 주의

Analytics 일별 집계가 **07-29 까지만** 나온다(2~3일 지연). 07-30~08-01 공개분의 "0회"는
일부가 집계 지연일 수 있다. 다만 Data API 의 `viewCount` 는 준실시간이고 그것도 0 이므로,
**지연만으로 설명되지는 않는다.**

### 8. 결론과 권고

**"성적 부진"이라기보다 개설 12일차·15편·구독자 3명인 채널이 아직 알고리즘 테스트를
통과하지 못한 상태다.** 파이프라인(생성·업로드·재생목록)은 정상 동작하고 기술 규격에도 문제가 없다.

권고 순서 —
1. **(먼저) Studio 에서 노출수·CTR 확인.** 이것 없이 콘텐츠를 고치는 건 추측이다.
2. 그 수치를 보고 나서 길이(11초)·첫 프레임·제목 중 무엇을 건드릴지 정한다.
3. 대기열이 **70편(약 23일치)** 남았다. 08-24 경 소진되므로 그 전에 대본을 보충한다.

**지금 코드를 고칠 이유는 찾지 못했다.**

### 9. (추가) Studio 실측값 수신 — 참여도 50.5%

Gim 이 YouTube Studio 앱 `콘텐츠 > Shorts` 화면을 캡처해 주었다(2026-08-02 00:48).

| 항목 | 값 |
|---|---|
| 조회수(28일) | 139 |
| **유효 조회수** | **61** (44%) |
| **시청자 참여도** | **계속 시청함 50.5% / 이탈함 49.5%** |
| 유입 | 채널 페이지 51.8% · YouTube 검색 29.5% · 직접입력/알수없음 5.8% |
| 검색 유입어 | **만리장성 · 갓생 사는 법 · 냉장고 파먹기** |

**`시청자 참여도` 가 영문 `Viewed vs. Swiped away` 다.** 쇼츠에는 CTR 이 없다(피드에서 넘기는
방식이라 클릭할 썸네일이 없음). 앞서 `youtube.py:14` 주석이 "노출수·클릭률"이라 적은 것은
장·단편 구분 없이 잘못 적힌 것이다.

**벤치마크 대조** — 70% 이상 = 확산 구간, **50/50 = 평균~정체**, 30% = 즉사.
swipe-away 30% 미만인 쇼츠는 50% 초과인 쇼츠보다 **약 4배의 지속 배포**를 받는다.
**Gim 채널은 49.5% 로, 배포가 확대되지 않는 구간의 경계에 정확히 있다.**
→ 앞서 실측한 "쇼츠 피드 유입 5%"의 원인이 이것으로 설명된다.

### 10. (추가) 검색 유입 29.5%는 곧 마른다 — 새로 발견

검색 유입어 3개가 **전부 지금 비공개로 내린 옛 갈래**(만리장성·갓생 사는 법·냉장고 파먹기)다.
현재 공개 중인 새 갈래로 들어오는 검색어는 목록에 하나도 없다.
**즉 유입의 29.5% 는 이미 사라진 영상이 남긴 잔열이고, 시간이 지나면 0 이 된다.**
현재 44회라는 조회수마저 실제보다 부풀려 보인다는 뜻이다.

### 11. (추가) 첫 문장이 전부 "결론 평서문"이다

공개된 15편의 첫 문장(= 첫 1~3초 자막)을 전수 확인했다.

```
하늘이 파란 건 파란빛이 잘 흩어지기 때문입니다.   ← 제목 '일상 속 숨은 과학'
바다의 소금은 수억 년 동안 쌓인 것입니다.        ← 제목 '바다는 왜 짤까'
딸꾹질은 횡격막이 갑자기 경련하는 것입니다.
단풍의 붉은색은 가을에 새로 만들어집니다.
```

**질문을 던지기 전에 답을 준다.** 제목이 질문이어도 첫 자막이 곧바로 정답이라 볼 이유가 사라진다.
11초라는 길이도 훅을 세울 여유를 남기지 않는다(4문장 74~93자를 밀어 넣음).

**단, 이것을 조회수로 입증하지는 못했다.** 공개 15편·총 44회는 표본이 너무 작고, 하위권 7편은
대부분 07-30 이후 업로드라 노출량 차이와 구분되지 않는다. **채널 전체 집계인 참여도 50.5% 만이
신뢰할 수 있는 신호**이며, 그것이 훅 문제를 가리킨다.

### 12. 개선안 (측정 가능한 최소 변경을 권고)

대기열 70편은 **이미 mp4 로 렌더링돼 있다.** 첫 문장을 바꾸려면 재렌더가 필요하고 TTS 과금이
붙는다. **1편당 소요·비용은 측정하지 않았다** — 무단 실행하면 과금되므로 Gim 승인 전에는 돌리지 않는다.

권고는 **다음 10편만 훅을 바꿔 올리고 2주 뒤 참여도를 비교**하는 것이다.
조건 하나만 바꿔야 원인을 확정할 수 있다([[controlled-comparison-measurement]]).
전량 재렌더는 효과가 확인된 뒤에 해도 늦지 않다.

### 13. 훅 개선 샘플 2편 제작 (Gim 지시: 샘플 먼저 보고 방향 재결정)

**원인 진단** — 대본 4줄의 구조가 `답 → 설명 → 설명 → 질문` 이다.
**질문이 마지막 줄에 있다.** 궁금증이 닫힌 뒤에 열리므로 첫 1~3초에 볼 이유가 없다.

**바꾼 것은 문장 순서 하나뿐이다.** 첫 줄을 답이 아니라 역설로 돌렸다.

| | 원본 첫 줄 | 샘플 첫 줄 |
|---|---|---|
| 179 바다는 왜 짤까 | 바다의 소금은 수억 년 동안 쌓인 것입니다 | **강물은 안 짠데 바다만 짭니다** |
| 202 잎이 붉게 물드는 이유 | 단풍의 붉은색은 가을에 새로 만들어집니다 | **곧 떨어뜨릴 잎에 나무가 새 색을 칠합니다** |

**대조 조건을 맞췄다** — 장면 4개, 글자 81·91자(원본 87·93자), 길이 10.40·11.13초
(원본 10.47·10.70초). 음성·배경·자막 스타일·재생목록 모두 동일. **훅만 다르다.**

- 대본: `samples/179h_…txt`, `samples/202h_…txt` (원본을 덮어쓰지 않았다)
- 영상: `previews/` 에 렌더 — **`output/` 에 넣지 않았다.** `upload_batch` 는 `output/*.mp4` 만
  훑으므로 이 샘플이 자동 업로드 대기열에 끼어들지 않는다.
- 비공개 업로드: 바다 <https://youtu.be/xJgMSIWXjSI> · 단풍 <https://youtu.be/3Dt3mcLstSg>

**오늘 밤 자동 업로드에 영향 없음을 확인했다** — `last_run.json` 은 `08-01` 그대로라
오늘 상한 계산이 0부터 시작한다. `uploaded.json` 도 73 그대로다.
API 할당량도 리셋이 16:00 KST 라 샘플 업로드(3,200 units)와 밤 23:00 실행이 다른 창에 들어간다.

**비용** — Google Neural2 TTS 172자. 월 100만 자 무료 한도 안이라 실질 0원.

---

## 2026-08-02(일) 12:4x — 샘플 피드백 3건 진단

Gim 검토 결과: ① 상단 자막과 가운데 훅 멘트가 겹쳐 거슬림 ② 반전에 맞춘 극적 연출 필요
③ 코믹·귀여운 장르 추가 검토.

### ① "겹침"의 정체 — 원인이 셋이다 (프레임 픽셀 실측)

샘플 영상에서 프레임을 뽑아 노란 자막 픽셀이 있는 세로 구간을 쟀다.

| 위치 | 글자 높이 | 줄 사이 여백 |
|---|---|---|
| 상단 내레이션 자막 | 88px, 86px | **2px** |
| 가운데 강조 문구 | 235px, 230px | **4px** |

**행간이 사실상 0이다.** 글자 높이의 2.3% · 1.7% 로, 앞줄 받침과 뒷줄 윗선이 맞닿는다.
읽기 편한 행간은 통상 글자 높이의 15~30%(여기선 13~26px · 35~70px)다.
원인은 ASS 스타일에 행간 항목이 없고 libass 가 폰트 line-height 를 그대로 쓰기 때문이다.
`H2HDRM`(HY헤드라인)은 한글이 em 을 거의 꽉 채우는 폰트라 줄이 붙는다.

**상단 자막과 가운데 강조는 물리적으로 겹치지 않는다** — 상단 y201~376, 강조 y723~1191,
사이 346px. 즉 "겹친다"의 절반은 **행간**이고, 나머지는 아래 두 가지다.

### ① -2 전환 0.4초 동안 두 장면 자막이 이중 노출된다

`make_video` 는 자막을 **클립마다 구워 넣은 뒤** `concat_xfade` 로 크로스페이드한다
(`video.py:780`). 따라서 전환 0.4초 동안 **앞 장면 자막과 뒤 장면 자막이 겹쳐 보인다.**
전환 프레임(6.6초)을 뽑아 보니 배경에 이전 자막 잔상이 그대로 남아 있었다.

**10.4초 영상에서 전환 3회 × 0.4초 = 1.2초, 전체의 11.5%** 가 이 상태다.

### ① -3 상단 자막과 강조가 같은 말이다 (설계상)

대본 3번째 칸(강조)이 그 문장의 요약이라 **같은 내용이 두 번** 나온다.

```
상단: 강이 바위 속 소금을 조금씩 실어 옵니다.
강조: 소금을 실어 온다          ← 같은 말
```

눈이 두 곳을 오가지만 새 정보가 없다. 이건 코드가 아니라 **대본 규칙**의 문제다.

### ② 극적 연출 — 현재는 모든 장면이 똑같다

`ZOOM_END 1.12` 켄번즈 확대와 크로스페이드가 **네 장면 모두 동일**하게 적용된다.
반전(마지막 장면)에만 주는 차별 연출이 없다. 강조 문구에 등장 애니메이션
(`\fscx70 → 104 → 100`, `video.py:496`)이 있으나 이것도 전 장면 공통이다.

### ③ 장르 추가에 대한 내 판단 — 지금은 이르다

- 이번에 이미 **훅·행간·전환·연출** 네 변수를 건드린다. 장르까지 더하면 무엇이 먹혔는지
  가릴 수 없다([[controlled-comparison-measurement]]).
- 유튜브는 채널 단위로 주제를 학습해 시청자를 매칭한다. 개설 12일차에 주제를 늘리면
  그 학습이 흐려진다. **다만 이건 통설이고 이 채널 데이터로 입증한 것은 아니다.**
- **대안** — "코믹·귀여운"을 별도 장르가 아니라 **같은 지식 콘텐츠의 톤**으로 적용하면
  주제는 유지하면서 표현만 바꾼다. 비용이 거의 없고 훅과 방향이 같다.

### 다음 작업 (Gim 결정 대기)

수정 우선순위 — (a) 전환 중 자막 이중노출 제거 (b) 행간 확보 (c) 반전 장면 전용 연출
(d) 강조 문구를 상단 자막과 다른 말로. **모두 화면이 바뀌므로 샘플 승인 후 반영한다**
([[gim-decides-fast-and-broadly]]).

### 14. v2 구현 — 겹침 3종 + 반전 연출 (Gim 승인 범위)

**`video.py` 변경**

| 무엇 | 어떻게 | 실측 결과 |
|---|---|---|
| 행간 | 줄마다 Dialogue 를 따로 쓰고 `MarginV` 로 내림(`SUB_LINE_PITCH=124`) | **2px → 37px** |
| 강조 행간 | `\an5\pos()` 로 줄마다 배치(`EMPHASIS_PITCH=1.22`) | 4px → 벌어짐 |
| 전환 이중노출 | 자막 종료를 `duration - XFADE` 로 당김 | 전환 프레임에서 자막 0 |
| 반전 연출 | `climax=True` — 하드컷 진입(`CLIMAX_CUT=0.08`) · 확대 +0.10 · 배경 -0.06 · 강조 펀치인(40→114→100) | 프레임으로 확인 |

`\N` 으로 줄을 나누면 libass 가 폰트 line-height 를 그대로 써 행간이 0 이 된다. ASS 스타일에
행간 항목이 없으므로 **줄마다 Dialogue 를 쓰는 것 말고는 방법이 없다.**

**중간에 잡은 회귀 — 자막이 말보다 먼저 걷혔다**

처음엔 `끝 = duration - XFADE` 로만 두었다. 그런데 `XFADE` 는 편 번호로 0.25~0.55 사이에서
정해지고 `GAP` 은 0.3 이라, **XFADE > GAP 인 편에서는 내레이션이 끝나기 전에 자막이 사라진다.**
179 편이 정확히 그랬다(XFADE 0.35 → 말보다 0.05초 먼저 걷힘, 페이드아웃까지 치면 0.45초).

프레임 스캔으로 발견했다 — 4.0초·6.4초에 자막이 비어 있었다. 고친 뒤 그 두 지점이 채워졌고
전환 지점(2.0·6.8초)만 비었다.

```python
단어끝 = max((s + d for s, d, _ in words), default=0.0)
끝 = min(duration, max(단어끝 + 0.15, duration - XFADE))
```

**말이 잘리는 것보다 겹침을 조금 감수하는 쪽이 낫다**는 판단이다.

**강조 문구 규칙 변경** — 문장의 요약이 아니라 한 겹 더 얹는 말로.

```
전) 강이 바위 속 소금을 조금씩 실어 옵니다. / 강조: 소금을 실어 온다   ← 같은 말
후) 강이 바위 속 소금을 조금씩 실어 옵니다. / 강조: 범인은 강
```

**테스트 3개 추가** (`test_video.py`) — 행간이 `SUB_LINE_PITCH` 간격으로 고른지 · `\N` 이
안 쓰이는지 · 전환 전에 걷히면서도 말이 끝나기 전에는 안 걷히는지 · 반전 연출이 달라지는지.
전체 테스트 통과.

### 15. 부수 확인 — 업로드 메타데이터의 `<` `>` 금지 문자

샘플 업로드가 `HttpError 400 invalidDescription` 으로 한 번 실패했다. 내가 쓴 설명글의
`->` 안에 든 `>` 때문이다. **YouTube 는 제목·설명에 `<` `>` 를 금지한다.**

본 업로드 경로도 위험한지 전수 확인했다 — `scripts/132_숫자로_보는_우주.txt` 에 `나무 > 별`,
`하루 > 1년` 이 있으나 **모두 3번째 칸(화면 강조 문구)이라 제목·설명·태그로 가지 않는다.**
대기열 전체에서 업로드 필드에 `<` `>` 가 섞이는 대본은 **0개**다. 조치 불필요.

**샘플 v2 (비공개)** — 바다 <https://youtu.be/j6gMMNLG1_8> · 단풍 <https://youtu.be/8QmSG1UV4-k>

---

## 2026-08-04(화) 10:1x — 뉴스 수집 사이트: 소스 실측 (신규 프로젝트 준비)

Gim 요청 = 관심분야 뉴스를 모아 GitHub Pages 로 띄우고 1시간마다 텔레그램 알림.
관심분야 확정 = **AI·클로드 / 게임 개발 / 유튜브·쇼츠 운영 / 최신 IT·IT 이슈**.

### 후보 42개를 실제로 접속해 확인했다 (추측 금지)

목록을 기억으로 적지 않고 `requests` 로 전수 조회해 **HTTP 상태·XML 파싱·항목 수·최신 제목**까지 확인했다.

**1차 27개 중 15개 생존.** 죽은 것들이 중요한 자리에 몰려 있었다.

| 죽은 소스 | 상태 |
|---|---|
| **Anthropic 뉴스·엔지니어링 RSS** | **HTTP 404 — 공식 RSS 가 없다** |
| 디스이즈게임 · 인벤 | 404 (국내 게임 매체 전멸) |
| ZDNet Korea(직접) · 블로터 · 디지털데일리 | 404 / 403 / 접속 실패 |
| Gamasutra 구주소 | 404 (Game Developer 로 이전) |
| Unreal 피드 | XML 뒤에 잡음이 붙어 파싱 실패 |

### 해결책 — 구글 뉴스 RSS 검색

`https://news.google.com/rss/search?q=<키워드>&hl=ko&gl=KR&ceid=KR:ko`

**키워드 하나로 국내외 매체를 한꺼번에 훑는다.** 개별 매체 RSS 가 죽어도 영향받지 않는다.
2차 실측 결과 5개 키워드 전부 정상(36~100건).

- 앤스로픽 OR 클로드 → 100건 · Anthropic OR "Claude AI"(영) → 100건
- 게임개발 OR 유니티 OR 언리얼 → 46건 · 유튜브 쇼츠 OR 알고리즘 → 36건 · IT 업계 OR 인공지능 → 42건

**Anthropic 이 공식 RSS 를 제공하지 않으므로, 클로드 소식은 이 경로가 유일한 실용안이다**
(보조로 `github.com/anthropics/claude-code/releases.atom` 10건 · HN 키워드 20건).

### 확정 소스 21개 (전부 생존 확인)

| 분야 | 소스 |
|---|---|
| AI·클로드 | 구글뉴스(한/영) · claude-code 릴리스 · HN 키워드 · OpenAI 뉴스 · Google AI 블로그 |
| 게임개발 | 구글뉴스 · Game Developer · Godot · Unity 블로그 |
| 유튜브·쇼츠 | 구글뉴스 · YouTube 공식 블로그 · YouTube Creators 채널 · TubeFilter |
| 최신IT | 구글뉴스 · ZDNet Korea(feedburner) · 전자신문 · IT동아 · The Verge · Ars Technica · TechCrunch · HN 프론트 · 네이버 D2 |

**파싱 함정 하나** — 일부 피드는 XML 문서 뒤에 잡음이 붙어 `ET.fromstring` 이 터진다
(`junk after document element`). 마지막 `</rss>`·`</feed>` 까지 잘라 내면 정상 파싱된다.

### 비용 (Haiku 4.5 실단가 확인)

입력 $1.00 / 출력 $5.00 per 1M. 기사 1건 ≈ 입력 300 + 출력 60 토큰 ≈ **$0.0006**.
하루 100건이면 **월 약 $1.8**. 앞서 "월 몇천원대"라 어림한 것보다 낮다.
Batches API 는 50% 싸지만 최대 24시간 지연이라 1시간 주기에는 부적합 — 쓰지 않는다.

### 결정 사항

- **API 키 없이 먼저 만든다.** 수집·페이지·알림은 키 없이 전부 동작하고, 키가 채워지면
  그 다음 갱신부터 요약이 붙게 짠다. Gim 의 Claude API 결제가 막혀 있어 이 순서가 필요하다.
- **텔레그램은 새 봇을 쓴다.** 업로드 알림(하루 1건·놓치면 안 됨)과 뉴스 알림(하루 24건)이
  한 방에 섞이면 업로드 실패가 묻힌다. 봇 생성은 **폰만으로 가능**하고,
  `chat.id` 는 내가 `getUpdates` 로 뽑는다(지난번 `update_id` 오독 재발 방지).

### 뉴스 브리핑 — 수집기 완성 (`axdata_15_news_brief`)

새 프로젝트 폴더 `D:\.CODE\AXdata\axdata_15_news_brief` 신설. 봇 `gim_news_brief_bot` 생성 확인
(`getMe` OK). **`chat.id` 는 아직 없다** — Gim 이 새 봇에게 말을 걸어야 나온다.

**실측으로 두 번 고쳤다.**

1. **한 소스가 전체를 뒤덮었다** — 첫 실행 1,781건 중 OpenAI 피드 하나가 1,107건(60%)이었다.
   그 피드는 아카이브 전량을 내준다. → 소스당 25건 상한 + 3일 이내 필터 → **268건**.
2. **⛔ 구글뉴스 검색은 "유튜브·쇼츠 운영"에 못 쓴다** — 한글·영문 쿼리 4종을 실측했는데
   전부 노이즈가 압도적이었다.

   ```
   "유튜브 수익"       → 심현섭, 하루 3억2천 벌고도…
   "유튜브 크리에이터"  → 당진서 논일하던 60대 온열질환 추정 사망  (매체명이 creatorjournal)
   "YouTube Shorts"  → Bucs' Rueben Bain Jr. Looks Like The Real Deal
   ```

   **구글 뉴스는 매체명까지 느슨하게 매칭한다.** 국내외 보도가 드문 좁은 실무 주제에서는
   관련 기사가 부족해 억지로 채운다. → 그 분야만 검색을 빼고 전용 매체 3종
   (YouTube 공식·YouTube Creators 채널·TubeFilter)으로 교체. **5건이지만 전부 정확**하다
   ("Are YouTube viewers treating long-form videos like Shorts?").

   **교훈** — 큐레이션된 전용 소스가 있으면 그것을 쓰고, 검색은 전용 소스가 아예 없는 곳
   (클로드·앤스로픽)에만 쓴다.

**현재 성적** — 231건 / 5초 / 실패 1(Hacker News 간헐 502·타임아웃, 다른 소스에 영향 없음).
IT이슈 124 · AI·클로드 71 · 게임개발 31 · 유튜브·쇼츠 5.

**파일** — `sources.py`(소스 23개) · `collect.py`(수집·중복제거·신규판정·상태저장).
`state.json` 에 기사 지문 4,000개까지 보관해 "이미 알린 것"을 거른다. 저장은 임시파일→교체라
쓰다 죽어도 원본이 남는다.

**남은 것** — 화면(HTML) · 텔레그램 알림 · 요약(API 키 대기) · 작업 스케줄러 등록 · 테스트.

### 뉴스 브리핑 — 공개 완료 (2026-08-04 10:40)

**라이브** — <https://gimaxax77-sys.github.io/news-brief/> (공개 저장소 `gimaxax77-sys/news-brief`)
**자동 실행** — 작업 스케줄러 `AXdata_NewsBrief`, **매시 05분**. 다음 11:05.
**알림** — 봇 `gim_news_brief_bot`, `chat.id=7403671803`.
번호가 업로드 봇과 같은 것은 그게 **Gim 의 텔레그램 사용자 ID** 라서다. 봇이 다르므로 방은 분리돼 있다.

**파일 12개** — `sources.py`·`collect.py`·`render.py`·`brief.py`·`test_brief.py`·`run_brief.bat`
·`CLAUDE.md`·`README.md`·`.env.example`·`.gitignore`·`requirements.txt`·`docs/index.html`.
`.env`·`state.json`·`brief_log.txt` 는 저장소에 없다.

#### 만들면서 실측으로 고친 것 4가지

1. **한 소스가 전체의 60%** — OpenAI 피드가 아카이브 1,107건을 통째로 내줬다.
   → 소스당 25건 상한 + 3일 필터. 1,781건 → 240건.
2. **구글뉴스는 좁은 실무 주제에 못 쓴다** — 매체명까지 느슨하게 매칭한다.
   `유튜브 크리에이터` → "당진서 논일하던 60대 온열질환 사망"(매체명이 `creatorjournal`).
   → 유튜브 분야만 검색을 빼고 전용 매체 3종으로. 5건이지만 전부 정확하다.
3. **같은 기사가 매체 수만큼 중복** — 구글뉴스 제목이 `제목 - 매체명` 이라 로이터판과
   재팬타임스판이 다른 기사로 잡혔다. → 매체명을 떼어 지문을 만들고,
   **출처를 그 매체명으로 표시**(`구글뉴스(한)` → `네이트`). 화면이 훨씬 읽기 쉬워졌다.
4. **⛔ `.bat` 은 CRLF + ASCII 여야 한다** — Write 도구가 LF 로 저장하자 `cmd` 가 줄을
   잘못 끊어 `set PYTHONIOENCODING` 이 `THONIOENCODING` 으로 반토막 났다.
   한글을 넣었더니 이번엔 `cmd`(cp949)와 파이썬(UTF-8)이 같은 로그에 섞여 써서 로그가 깨졌다.
   → **배치 안의 글자는 전부 영문**, 바이트를 직접 지정해 CRLF 로 기록.
   같은 실수를 axdata_13 에서 안 겪은 이유는 그 배치가 윈도우에서 만들어져 원래 CRLF 였기 때문이다.

#### 검증

- `python -X utf8 -u test_brief.py` → **21항목 통과**(망 접속 없이 돈다)
- `run_brief.bat` 실제 실행 → exit 0 · 로그 깨짐 없음 · 푸시까지 확인
- 라이브 페이지 `curl` → HTTP 200 · 240건 · 머리말 "2026-08-04 10:38 기준"

#### 남은 것

- **한줄요약** — `ANTHROPIC_API_KEY` 가 비어 있어 꺼져 있다. 키를 넣으면 그 다음 갱신부터 붙는다.
- 첫 실행이라 "새 소식"이 많이 잡혔다. 다음 갱신부터는 실제 신규만 나온다.

### 뉴스 브리핑 — 검색·색인·시인성 (2026-08-04 10:50)

**검색** — 상단 고정 입력창. 기사마다 `data-k`(제목+출처+요약을 소문자로 이어 둔 문자열)를
심어 두고 순회하며 거른다. 241건은 즉시 걸러진다.
⚠ **한글은 `input` 만으로는 마지막 글자가 안 잡힌다** — IME 조합 중에는 이벤트가 안 오므로
`compositionend` 도 함께 듣는다.

**색인** — 출처별 칩(많이 실린 순, 건수 표시). 눌러서 그 매체만 본다. 14곳까지 보이고
나머지는 "더 보기". 분야 칩도 같은 방식. 항목이 다 사라진 분야는 `<section>` 째로 숨겨
빈 제목만 남지 않게 했다.

**시인성** — 제목 15.5→**18px/700**, 본문 16→**17px**, 요약 13.5→**15px**, 행간·자간 상향.
한글 폰트를 **Pretendard**(jsDelivr 동적 서브셋)로. 못 받아 오면 시스템 폰트로 내려앉으므로
페이지가 깨지지 않는다.

#### 검증 — 파이썬 테스트로는 부족해 브라우저에서 실제로 눌렀다

검색·필터는 자바스크립트라 `test_brief.py`(29항목)로는 표시 로직까지 확인할 수 없다.
라이브 페이지에서 직접 조작해 실측했다.

| 조작 | 결과 |
|---|---|
| 기본 | 241건 |
| 검색 "클로드" | **16건** · 머리말 건수도 16으로 동기화 · 보이는 분야 1개(빈 분야 숨김 정상) |
| 없는 말 | 0건 + "검색 결과가 없습니다" 표시 |
| 지우기(×) | 241건 복구 |
| 분야칩 "게임개발" | 31건 → 해제 시 241건 |
| 출처칩 "Hacker News(34)" | **34건** — 칩에 적힌 수와 정확히 일치 |
| 폰트·크기 | `Pretendard Variable` · 본문 17px · 제목 18px |

**함정 하나** — 브라우저 콘솔로 검사할 때 페이지가 이미 `const q` 를 전역에 선언해 두어
같은 이름을 쓰면 `Identifier 'q' has already been declared` 로 죽는다. 검사 코드는
즉시실행함수로 감싸고 변수명을 겹치지 않게 쓸 것.

**Pages 배포 지연** — 푸시 후 즉시 `curl` 하면 옛 버전이 온다. 새 마크업이 포함됐는지
(`grep -c 'id="q"'`) 로 확인하고 나서 검사할 것.

### 뉴스 브리핑 — 분야별 색 카드 (2026-08-04 10:55)

기사 항목을 카드로 바꾸고 분야 색을 아주 옅게 깔았다. **같은 색을 분야 칩의 점과 구역 제목에도
물려**, 색과 분야가 자연스럽게 학습되게 했다(색인을 눈으로 익히는 장치).

| 분야 | 색 |
|---|---|
| AI·클로드 | `#3b6fd4` 파랑 |
| 게임개발 | `#8b5cf6` 보라 |
| 유튜브·쇼츠 | `#e11d48` 빨강 |
| IT이슈 | `#0d9488` 청록 |

색상환에서 멀리 떨어진 넷을 골라 **옅어져도 서로 구분된다.**

**구현** — `분야색` 사전에서 CSS 를 생성한다(`_색규칙()`). 분야를 추가해도 CSS 를 따로
고칠 필요가 없고, 목록에 없는 분야는 무채색으로 나온다. 배경은
`color-mix(in srgb, var(--tint) 6%, transparent)` — **color-mix 를 못 쓰는 낡은 브라우저에서는
배경만 빠지고 글은 그대로 읽힌다**(`background:transparent` 를 먼저 선언해 둠).

**브라우저 실측 (라이브 페이지에서 직접)**

| 항목 | 밝은 모드 | 어두운 모드 |
|---|---|---|
| 카드 배경 투명도 | **6%** | 13% |
| 네 분야가 서로 다른 색인가 | — | **4/4 고유** |
| 본문 글자 대비 | **15.5 ~ 15.9 : 1** | (밝은 글자, 동일 규칙) |

WCAG AAA 기준이 7:1 이므로 **두 배 이상 여유**가 있다. "아주 옅게"라는 요구와 가독성이
충돌하지 않음을 수치로 확인했다.

빈 분야 안내(`li.empty`)에는 카드를 씌우지 않는다 — 기사가 아닌데 기사처럼 보이면 안 된다.

테스트 37항목 전체 통과.

---

# 2026-08-04 — 샘플 v2 전량 적용, 재렌더 결과 검증

## 무엇을 왜 했나

Gim 이 v2 샘플을 승인해 **대기열 64편 전량에 적용**했다. 적용은 두 갈래였다.

| 갈래 | 내용 | 방법 |
|---|---|---|
| **A. 화면** | 자막 행간 · 전환 겹침 · 반전 연출 | `video.py` 는 이미 커밋(`df73d77`). **재렌더만** 하면 반영 |
| **B. 훅** | 첫 줄이 답부터 말하는 대본 교정 | 대본 파일 수정(`3310593`) 후 재렌더 |

훅은 **64편 전부가 아니라 20편(31%)만** 고쳤다. 나머지 44편은 이미 훅이 강했다
("별은 사실 반짝이지 않습니다"). 전부 고쳤으면 멀쩡한 44편을 망칠 뻔했다.

## 재렌더 결과 — 64/64 성공

```
완료 64편 · 실패 0편 · 19.9분
```

- 잔여 임시파일 `*__new.mp4` **0개** · `output/` mp4 **215개** (손실 없음)
- 사전 실측대로였다(1편 20초 → 64편 21분 예상, 실제 19.9분).
- TTS 5,952자 — 월 100만 자 무료 한도 안. **비용 0원**.

## 검증 ① 길이 — "절반으로 줄었다"는 착시였다

전수 측정에서 재렌더본 평균 **11.2초**, 이미 업로드된 기존본 평균 **21.4초** 로 나와
회귀를 의심했다. 원인을 끝까지 따라가니 **재렌더 탓이 아니었다.**

| | 장면 수 | 나레이션 | 영상 길이 |
|---|---|---|---|
| 기존 업로드본(옛 대본) | 5장면 | 136자 | 21.4초 |
| 재렌더 대상(현 대본) | 4장면 | 90자 | 11.2초 |

**대본 세대가 다르다.** 결정적 증거는 같은 대본의 전/후 비교다
(`previews/_before_100_밤하늘_별자리.mp4`).

```
100_밤하늘_별자리: 전 11.7초 → 후 11.2초
```

−0.5초는 **마지막 전환을 크로스페이드(0.25~0.55초)에서 하드컷(0.08초)으로 바꾼**
v2 변경분과 정확히 들어맞는다. 의도한 변화다.

> ⚠ **평균끼리 비교하면 안 된다.** 모집단이 다르면 차이가 처리 효과처럼 보인다.
> 전/후를 같은 대본으로 물어야 답이 나온다.

## 검증 ② 화면 — 행간·겹침 해결 확인

`100_밤하늘_별자리` 를 0.4초 간격 27프레임으로 뽑아 픽셀 측정했다.

| 항목 | v1 | v2 실측 |
|---|---|---|
| 자막 줄 간격 | **2px** | **26~46px** (설계값 37px 부근) |
| 상단 자막 ↔ 중앙 강조 간격 | 겹침 | **123~170px** |

프레임 육안 확인에서도 두 자막이 서로 침범하지 않는다.

**"자막 없는 프레임 6개"는 오탐이었다.** 밝기 임계값 235 로 걸렀는데, 그 프레임들은
자막이 사라진 게 아니라 **페이드아웃 중**이라 최대 밝기가 임계값 아래였다. 실제로 열어 보니
글자가 또렷이 있다. 임계값 하나로 "없다"고 단정하면 안 된다.

## 확인하지 못한 것

- **반전 연출(확대 +10% · 어둡게 +6% · 하드컷)** 은 정지 프레임으로 판별이 어렵다.
  코드 차원에서는 `test_반전장면은_연출이_다르다` 가 검증한다. 실제 체감은 재생해 봐야 안다.
- **훅 개선이 참여도를 올리는지** 는 아직 모른다. 기준값 **50.5%**, 2주 뒤 재측정 대상.

## 발견 — 강조 자막과 문장의 낱말 겹침 67%

강조 자막 192개 중 **129개(67%)** 가 같은 줄 문장의 낱말을 그대로 쓴다.

```
"한 별자리의 별들은 서로 남남입니다."  →  강조 "사실은 남남"
"문어는 심장이 세 개입니다."           →  강조 "심장 3개"
```

**영향 범위** — 화면 오작동은 아니다. 자막은 정상 렌더된다. 순수한 연출 취향 문제다.

**판단** — 고칠 값어치가 있는지 **단정하지 않는다.** 쇼츠 문법에서 핵심어를 크게 반복하는 것은
정석에 가깝고("심장 3개"는 요약이지 잡음이 아니다), Gim 이 지적한 "겹친다"는 지적은 맥락상
**화면 위치**였고 그것은 이미 해결했다. 내용 중복까지 문제로 확대 해석해 129곳을 고치면
멀쩡한 연출을 망칠 위험이 크다. **Gim 이 실물을 보고 판단할 사안**으로 남긴다.

## 테스트

```
test_generate.py    통과 (주소 5 · 문장합치기 2 · 시간표기 2)
test_check_upload.py 통과 12/12
test_video.py       통과
test_youtube.py     통과
```

